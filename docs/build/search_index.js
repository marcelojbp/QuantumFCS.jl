var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#api","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Computing-cumulants","page":"API","text":"","title":"Computing cumulants"},{"category":"section","location":"api/#Problem-types","page":"API","text":"","title":"Problem types"},{"category":"section","location":"api/#Steady-state","page":"API","text":"Package-level helpers to solve for the trace-constrained steady state and reuse the preconditioner built for that solve in the iterative FCS backend (see Preparing the steady state for iterative FCS):","title":"Steady state"},{"category":"section","location":"api/#Drazin-inverse-helpers","page":"API","text":"The prepared Drazin solvers used internally are documented on the Drazin solvers page. The lower-level building blocks are:","title":"Drazin inverse helpers"},{"category":"function","location":"api/#QuantumFCS.fcscumulants_recursive","page":"API","text":"fcscumulants_recursive(L, mJ, nC, rho_ss, nu; cumulant_type=\"\")\nfcscumulants_recursive(H, J, mJ, nC, rho_ss, nu; cumulant_type=\"\")\n\nCalculate n-th zero-frequency cumulant of full counting statistics using a recursive scheme.\n\nArguments\n\nL: Vectorized Liouvillian matrix (sparse or dense, ComplexF64)\n\nAlternatively, one can provide the Hamiltonian and jump operators instead of L\n\nH: Hamiltonian operator (sparse or dense, Operator from QuantumOptics.jl)\nJ: Vector of jump operators (sparse or dense, Operator from QuantumOptics.jl)\nmJ: Vector containing the monitored jump matrices (sparse operators in vectorized representation).\nnC: Number of cumulants to be calculated.\nrho_ss: Steady-state density matrix (sparse or dense, ComplexF64)\nnu: Vector of length length(mJ) with weights for each jump.\n\nKeyword arguments\n\nmethod: Drazin-solve backend, :lu (default) or :iterative.\nσ, τ, rtol, itmax, memory: options for the :iterative backend (see prepare_drazin_solver); ignored by :lu.\nPl: an externally built preconditioner reused by the :iterative backend instead of building an ILU internally (see prepare_drazin_solver). nothing (default) builds the internal preconditioner; ignored by :lu and by the dense-Liouvillian method.\ncumulant_type: Optional keyword. Use \"\" (default) for ordinary cumulants, or \"factorial\" to convert the result with factorial_cumulants.\n\n\n\n\n\nfcscumulants_recursive(problem::FCSProblem)\n\nSolve a full-counting-statistics problem, returning its first problem.nC zero-frequency cumulants. Equivalent to calling the positional method with the problem's fields, after normalizing any backend operators to their underlying matrices.\n\n\n\n\n\nfcscumulants_recursive(ctx::PreparedLindbladFCS; mJ, nu, nC=2, cumulant_type=\"\")\n\nEvaluate a full-counting-statistics observable on a prepared context, reusing its Drazin solver. Returns the first nC zero-frequency cumulants.\n\nmJ are the monitored jump operators/matrices (backend operators are accepted and normalized) and nu their weights (length(nu) == length(mJ)). Each jump must be n×n, where n is the side of the context's steady state. See prepare_fcs_context for the \"prepare once, evaluate many observables\" workflow and fcscumulants_recursive for cumulant_type.\n\n\n\n\n\n","title":"QuantumFCS.fcscumulants_recursive"},{"category":"function","location":"api/#QuantumFCS.factorial_cumulants","page":"API","text":"factorial_cumulants(cumulants::AbstractVector{<:Number})\n\nConvert ordinary cumulants [c1, c2, ..., cn] to factorial cumulants [f1, f2, ..., fn].\n\nThe conversion uses the signed Stirling numbers of the first kind,\n\nf_m = sum(s(m, j) * c_j for j in 1:m)\n\nwith s(1, 1) = 1 and the recurrence s(m, j) = s(m - 1, j - 1) - (m - 1) * s(m - 1, j).\n\nThe physical factorial-cumulant interpretation assumes dimensionless count cumulants. Dimensionful heat-current cumulants should be rescaled first.\n\n\n\n\n\n","title":"QuantumFCS.factorial_cumulants"},{"category":"type","location":"api/#QuantumFCS.FCSProblem","page":"API","text":"FCSProblem\n\nAbstract supertype for full-counting-statistics problems. A concrete subtype encapsulates everything fcscumulants_recursive needs — the Liouvillian (or the Hamiltonian/jump operators it is built from), the monitored jumps, the steady state, the weights, and the number of cumulants — so that a simulation can be organised as a single object and solved with fcscumulants_recursive(problem).\n\n\n\n\n\n","title":"QuantumFCS.FCSProblem"},{"category":"type","location":"api/#QuantumFCS.LindbladFCS","page":"API","text":"LindbladFCS(; H=nothing, J=nothing, L=nothing, mJ, rho_ss, nu, nC=2)\n\nA full-counting-statistics problem for a Lindblad master equation.\n\nConstruct it by keyword. Either supply a (vectorized) Liouvillian L, or both a Hamiltonian H and a vector of jump operators J (in which case L is built from them at solve time by the active backend). Fields may be plain dense/sparse ComplexF64 arrays, or backend operators (QuantumOptics.Operator, QuantumToolbox.QuantumObject); the relevant package extension takes care of extracting the underlying matrices.\n\nFields\n\nH: optional Hamiltonian operator (backend type) used to build L.\nJ: optional vector of jump operators (backend type) used to build L.\nL: optional (vectorized) Liouvillian. Takes precedence over H/J.\nmJ: vector of monitored jump operators/matrices.\nrho_ss: steady-state density matrix.\nnu: weights, one per monitored jump (length(nu) == length(mJ)).\nnC: number of cumulants to compute (default 2).\nmethod: Drazin-solve backend, :lu (default) or :iterative.\nσ, τ, rtol, itmax, memory: options for the :iterative backend (diagonal shift, ILU drop tolerance, Krylov tolerance, iteration cap, and GMRES restart memory). Ignored by :lu. σ=nothing auto-scales the shift from L.\nPl: an externally built preconditioner for the :iterative backend to reuse instead of building its own ILU (e.g. the ILU from the steady-state solve). See prepare_drazin_solver for the contract it must satisfy. nothing (default) builds the internal preconditioner; ignored by :lu.\n\nSolve with fcscumulants_recursive:\n\np = LindbladFCS(; H=H, J=J, mJ=[Jc], rho_ss=ρss, nu=[1], nC=2)\nc1, c2 = fcscumulants_recursive(p)\n\n# large sparse Liouvillian (needs `using Krylov, IncompleteLU`):\np = LindbladFCS(; L=L, mJ=[Jc], rho_ss=ρss, nu=[1], nC=3, method=:iterative)\n\n\n\n\n\n","title":"QuantumFCS.LindbladFCS"},{"category":"type","location":"api/#QuantumFCS.PreparedLindbladFCS","page":"API","text":"PreparedLindbladFCS\n\nA reusable full-counting-statistics context: the observable-independent data for a fixed Liouvillian and steady state, with a Drazin solver prepared once.\n\nBuild it with prepare_fcs_context and evaluate any number of observables that share the same L and rho_ss with\n\nfcscumulants_recursive(ctx; mJ, nu, nC=2)\n\nBecause the monitored jumps mJ and weights nu enter only the counting-field super-operators and the recursion's right-hand sides — never the Drazin linear algebra — a single prepared solver serves every observable, so the expensive preparation (sparse LU factorization, or shifted-ILU build for the iterative backend) is done only once.\n\nThis complements LindbladFCS: use LindbladFCS for a single observable, and a PreparedLindbladFCS context when many observables share one Liouvillian and steady state.\n\nFields\n\nL: the (sparse ComplexF64) vectorized Liouvillian.\nrho_ss: the (sparse ComplexF64) steady-state density matrix, kept for introspection and dimension checks.\nvrho_ss: the normalized, vectorized steady state (right null vector).\nvId: the vectorized identity / trace functional (left null vector).\nsolver: the prepared DrazinSolver reused across observables.\n\n\n\n\n\n","title":"QuantumFCS.PreparedLindbladFCS"},{"category":"function","location":"api/#QuantumFCS.prepare_fcs_context","page":"API","text":"prepare_fcs_context(; H=nothing, J=nothing, L=nothing, rho_ss,\n                    method=:lu, σ=nothing, τ=0.05, Pl=nothing,\n                    rtol=1e-8, itmax=200, memory=30) -> PreparedLindbladFCS\n\nPrepare a reusable PreparedLindbladFCS context for computing many full-counting-statistics observables that share the same Liouvillian and steady state.\n\nSupply either a (vectorized) Liouvillian L, or both a Hamiltonian H and jump operators J (built into L by the active backend). L/H/J/rho_ss may be plain dense/sparse ComplexF64 arrays or backend operators (QuantumOptics.Operator, QuantumToolbox.QuantumObject); the relevant package extension extracts the underlying matrices. The Drazin solver is prepared once here and then reused by every call to fcscumulants_recursive(ctx; mJ, nu, nC).\n\nThe solver-backend keywords are identical to those of LindbladFCS / fcscumulants_recursive and configure this one-time preparation:\n\nmethod: :lu (default) or :iterative (needs the QuantumFCSIterativeExt extension, using Krylov, IncompleteLU).\nσ, τ, Pl, rtol, itmax, memory: options for the :iterative backend, forwarded to prepare_drazin_solver; ignored by :lu. Pl supplies an externally built preconditioner for the iterative backend to reuse.\n\nExample\n\nctx  = prepare_fcs_context(; L = L, rho_ss = ρss, method = :lu)   # prepares LU once\nhot  = fcscumulants_recursive(ctx; mJ = mJ_hot,  nu = nu_hot,  nC = 2)\ncold = fcscumulants_recursive(ctx; mJ = mJ_cold, nu = nu_cold, nC = 2)\n\n\n\n\n\nprepare_fcs_context(ss::TraceConstrainedSteadyState; method = :iterative,\n                    σ = nothing, τ = 0.05, rtol = 1e-8, itmax = 200, memory = 30)\n\nBridge a solved TraceConstrainedSteadyState into a reusable PreparedLindbladFCS context, forwarding the steady-state preconditioner so the iterative FCS backend does not rebuild an ILU.\n\nWith method = :iterative the steady-state ss.Pl is passed through as the FCS preconditioner; with method = :lu the direct backend is used and Pl is ignored. The steady state is not recomputed. The σ/τ/rtol/itmax/memory keywords tune the FCS Drazin solve exactly as in the keyword prepare_fcs_context.\n\n\n\n\n\n","title":"QuantumFCS.prepare_fcs_context"},{"category":"function","location":"api/#QuantumFCS.trace_constrained_steadystate","page":"API","text":"trace_constrained_steadystate(L; method = :iterative, kwargs...)\ntrace_constrained_steadystate(H, J; method = :iterative, kwargs...)\ntrace_constrained_steadystate(sys::TraceConstrainedSystem; method = :iterative, kwargs...)\n\nSolve for the trace-constrained steady state, returning a lean TraceConstrainedSteadyState that carries rho_ss and the reusable preconditioner Pl.\n\nmethod = :lu solves the sparse system directly (A \\ b) and returns Pl = nothing — a reliable baseline for small/medium systems. method = :iterative builds a shifted-ILU preconditioner and solves with GMRES (requires the QuantumFCSIterativeExt extension); the resulting Pl is meant to be reused by the iterative FCS backend.\n\nIterative keyword arguments (ignored by :lu):\n\nPl — reuse an externally built preconditioner instead of building one.\nu0 — initial guess for GMRES (warm start, e.g. a neighbouring solution).\nτ, shift_factor, shift — shifted_ilu_preconditioner options.\nrtol (1e-10), atol (1e-14), itmax (200), memory (60) — GMRES tolerances, iteration cap, and Krylov basis size (defaults match the applications).\n\nPass the low-level sys::TraceConstrainedSystem form to reuse a prebuilt system and preconditioner across a continuation sweep without rebuilding either.\n\n\n\n\n\n","title":"QuantumFCS.trace_constrained_steadystate"},{"category":"type","location":"api/#QuantumFCS.TraceConstrainedSteadyState","page":"API","text":"TraceConstrainedSteadyState\n\nThe lean, user-facing result of trace_constrained_steadystate: a steady state together with the preconditioner built to find it, ready to feed into the iterative FCS machinery via prepare_fcs_context.\n\nIt intentionally does not retain the large trace-constrained matrix A or the right-hand side b — the target problems are memory critical.\n\nFields\n\nL: the sparse ComplexF64 Liouvillian used for FCS.\nrho_ss: the steady state as a sparse ComplexF64 matrix, hermitianized and trace-normalized.\nPl: the preconditioner built for the steady-state solve (nothing for the direct :lu method). Reused by the :iterative FCS backend — this reuse is the whole point of the API.\nstats: a NamedTuple of scalar diagnostics only (convergence, iterations, residuals, trace/hermiticity errors, ILU/GMRES timings) — no large arrays.\n\n\n\n\n\n","title":"QuantumFCS.TraceConstrainedSteadyState"},{"category":"function","location":"api/#QuantumFCS.trace_constrained_system","page":"API","text":"trace_constrained_system(L; weight = nothing) -> TraceConstrainedSystem\ntrace_constrained_system(H, J; weight = nothing) -> TraceConstrainedSystem\n\nBuild the trace-constrained steady-state linear system for a Liouvillian.\n\nSupply a (vectorized) Liouvillian L, or a Hamiltonian H and jump operators J (built into L by the active backend). Inputs may be plain dense/sparse ComplexF64 arrays or backend operators (QuantumOptics, QuantumToolbox); they are normalized to SparseMatrixCSC{ComplexF64, Int}.\n\nThe default constraint weight is norm(L, 1) / length(L) (the entrywise 1-norm of L divided by its total number of entries), matching the application helpers; pass weight to override it.\n\n\n\n\n\n","title":"QuantumFCS.trace_constrained_system"},{"category":"type","location":"api/#QuantumFCS.TraceConstrainedSystem","page":"API","text":"TraceConstrainedSystem\n\nThe trace-constrained steady-state linear system A ρ⃗ = b for a Liouvillian L.\n\nL is singular (the physical steady state is its null vector), so the redundant first equation is replaced by a trace constraint: A = L + |row 1⟩⟨diag|·w and b = w·e₁, where the constraint hits the vectorized-identity (diagonal) indices so that solving imposes tr(ρ) = 1.\n\nThis is a low-level object for constructing, testing, and warm-started continuation solves. Prefer the lean TraceConstrainedSteadyState as the user-facing result. Build one with trace_constrained_system.\n\nFields\n\nL: the sparse ComplexF64 Liouvillian (unmodified; this is what FCS uses).\nA: the trace-constrained system matrix L + trace_constraint.\nb: the right-hand side (w in the first entry, zeros elsewhere).\nvId: the vectorized identity / trace functional (diagonal indices).\ndimensions: backend Hilbert-space dimensions when available, else nothing.\n\n\n\n\n\n","title":"QuantumFCS.TraceConstrainedSystem"},{"category":"function","location":"api/#QuantumFCS.shifted_ilu_preconditioner","page":"API","text":"shifted_ilu_preconditioner(A; τ = 1e-3, shift_factor = 1e-6, shift = nothing)\n\nBuild a shifted incomplete-LU preconditioner for the trace-constrained matrix A.\n\nA small diagonal shift keeps the incomplete factorization well-behaved: ilu(A + shift·I; τ). If shift is nothing it is scaled from the operator as shift_factor · max(norm(A, 1) / size(A, 1), eps(Float64)), matching the application helpers. The shift affects only the preconditioner, never the solved system.\n\nRequires the QuantumFCSIterativeExt extension (using Krylov, IncompleteLU).\n\n\n\n\n\n","title":"QuantumFCS.shifted_ilu_preconditioner"},{"category":"function","location":"api/#QuantumFCS.drazin","page":"API","text":"drazin(L, vrho_ss, vId, IdL)\n\nCalculate the Drazin inverse of a Liouvillian defined by the Hamiltonian H and jump operators J.\n\nArguments\n\nL : Liouvillian matrix\nvrho_ss: vectorised density matrix specifying the steady-state of the Liouvillian.\nvId: vectorised identity matrix (1×N row or vector)\nIdL: Identity matrix in Liouville space (N×N)\n\nReturns\n\nA dense matrix holding the (projected) Drazin inverse Lᴰ of L.\n\nnote: Note\nThis builds the full Drazin inverse via a dense pseudoinverse and is intended for small systems or reference checks. For computing cumulants prefer prepare_drazin_solver / drazin_solve, which only apply the inverse to the right-hand sides actually needed.\n\n\n\n\n\n","title":"QuantumFCS.drazin"},{"category":"function","location":"api/#QuantumFCS.drazin_apply","page":"API","text":"drazin_apply(L, α, ρ, vId; F=nothing, rtol=1e-12, atol=0.0)\n\nApply the (projected) Drazin inverse of the Liouvillean L to the vector α by solving a linear system.\n\nArguments\n\nL: Liouvillean operator (matrix).\nα: Right-hand side vector.\nρ: Steady-state vector.\nvId: Vectorized identity vector.\nF: Optional factorization of L to reuse (default: nothing).\nrtol: Relative tolerance for the solver (default: 1e-12).\natol: Absolute tolerance for the solver (default: 0.0).\n\nReturns\n\nA (sparse) vector representing the result of applying the projected Drazin inverse.\n\n\n\n\n\n","title":"QuantumFCS.drazin_apply"},{"category":"function","location":"api/#QuantumFCS.m_jumps","page":"API","text":"m_jumps(mJ; n=1, nu=vcat(fill(+1, length(mJ)÷2), fill(-1, length(mJ)÷2)))\n\nCalculate the vectorized super-operator ℒ(n) = ∑ₖ (νₖ)ⁿ (Lₖ*)⊗Lₖ.\n\nArguments\n\nmJ: List of monitored jumps\nn : Power of the weights νₖ. By default set to 1, since this case appears more often.\nnu: vector of length length(mJ) with weights for each jump operator.\n\n\n\n\n\n","title":"QuantumFCS.m_jumps"},{"category":"section","location":"quickstart/#quickstart","page":"Quickstart","text":"This package computes full-counting-statistics cumulants from Lindblad dynamics. A model is defined by a Hamiltonian and jump operators, or by a preassembled Liouvillian; a current is defined by monitored jumps mJ and weights nu.","title":"Quickstart"},{"category":"section","location":"quickstart/#Installation","page":"Quickstart","text":"To install the package, in the Julia REPL, \n\nusing Pkg \nPkg.add(\"QuantumFCS\")","title":"Installation"},{"category":"section","location":"quickstart/#Quickstart-example","page":"Quickstart","text":"We model a quantum dot heat engine as a single mode coupled to two reservoirs (hot and cold) in the large bias limit (n_c = 0 n_h = 1). We monitor the cold-reservoir loss channel and assign it weight +1, so the first cumulant is positive for this chosen current orientation.\n\nusing QuantumOptics \nusing QuantumFCS\n# Basis for the single quantum dot\nb = FockBasis(1)\n# Operators \nd = destroy(b)\nd_dag = create(b)\n# Parameters\nϵd = 1.0;                            # Energy level of the quantum dot\nκc = 0.1;                            # Coupling strength to cold reservoir\nκh = 0.5;                            # Coupling strength to hot reservoir\n# Hamiltonian and jump operators\nH = ϵd * d_dag * d;                  \nJcloss = sqrt(κc) * d;               # Jumps into the cold reservoir \nJhgain = sqrt(κh) * d_dag;           # Jumps from the hot reservoir\nJ = [Jcloss, Jhgain];\n# Steady state\nρss = steadystate.iterative(H, J)\n# Weight vector: +1 for each monitored cold-reservoir jump\nnu = [1];\n# Monitored jump operator\nmJ = [Jcloss];\n# Calculating the first two cumulants\nc1, c2 = fcscumulants_recursive(H, J, mJ, 2, ρss, nu);\nprintln(\"\\nFull Counting Statistics:\")\nprintln(\"First cumulant : $c1\")\nprintln(\"Second cumulant : $c2\") ","title":"Quickstart example"},{"category":"section","location":"quickstart/#Without-a-quantum-framework","page":"Quickstart","text":"You do not need QuantumOptics.jl (or QuantumToolbox.jl). If you already have a vectorised Liouvillian L, its steady state ρss, and your monitored jump operators mJ, call the core method directly:\n\nusing QuantumFCS\n\n# L    : ComplexF64 sparse/dense matrix — the vectorized Liouvillian\n# mJ   : Vector of monitored jump operators (matrices, not super-operators)\n# ρss  : steady-state density matrix\n# nu   : weights, one per entry of mJ\nc1, c2, c3 = fcscumulants_recursive(L, mJ, 3, ρss, nu)\n\nSee the Examples for a full manual Liouvillian construction.","title":"Without a quantum framework"},{"category":"section","location":"quickstart/#Next-steps","page":"Quickstart","text":"For large sparse Liouvillians, switch to the matrix-free iterative backend with fcscumulants_recursive(...; method = :iterative) — see Drazin solvers.\nTo bundle a problem with its solver options, use the LindbladFCS problem type and call fcscumulants_recursive(problem).\nTo reuse solver setup across several currents of the same system, build a PreparedLindbladFCS context with prepare_fcs_context.","title":"Next steps"},{"category":"section","location":"examples/#examples","page":"Overview","text":"","title":"Examples"},{"category":"section","location":"examples/#Quantum-dot-heat-engine","page":"Overview","text":"We study a quantum dot coupled to two fermionic reservoirs, for details on the model see Patrick P. Potts, 2024. \n\nusing QuantumFCS, LinearAlgebra, SparseArrays\n\n\nDefine parameters and system\n\n# System parameters\nϵd = 1.0;  # Energy level of the quantum dot\nκc = 0.1;  # Coupling strength to cold reservoir\nκh = 0.5;  # Coupling strength to hot reservoir\nnc = 0.0;  # Occupation number of cold reservoir\nnh = 1.0;  # Occupation number of hot reservoir\n\n# Basis states and identity \noccupied = complex.([0,1]);\nempty = complex.([1,0]);\nid = sparse(1.0I,2,2);\n\n## Operators\n# Hamiltonian \nH = sparse(ϵd * (occupied * occupied'))  \n# Jump operators\nJcloss = sparse(sqrt(κc * (1-nc)) * (empty * occupied'));\nJcgain = sparse(sqrt(κc * nc) * (occupied * empty'));\nJhgain = sparse(sqrt(κh * nh) * (occupied * empty'));\nJhloss = sparse(sqrt(κh * (1-nh)) * (empty * occupied'));\n\nWe construct the Liouvillian \n\n## Constructing the Liouvillian\n# Unitary part \nL = -im * (kron(id,H) - kron(transpose(H),id))\n# Jump terms from the dissipators \nL += kron(conj(Jcloss),Jcloss) + kron(conj(Jhloss),Jhloss) + kron(conj(Jhgain),Jhgain)\n# Non-jump terms from the dissipators\nL += (-1/2 * kron(id,Jcloss'*Jcloss) \n     -1/2 * kron(transpose(Jcloss'*Jcloss),id) \n     -1/2 * kron(id,Jcgain'*Jcgain) \n     -1/2 * kron(transpose(Jcgain'*Jcgain),id) \n     -1/2 * kron(id,Jhloss'*Jhloss) \n     -1/2 * kron(transpose(Jhloss'*Jhloss),id)\n     -1/2 * kron(id,Jhgain'*Jhgain) \n     -1/2 * kron(transpose(Jhgain'*Jhgain),id))  \n\nand determine the steady state \n\n # Determining the steady state\nE = eigen(Matrix(L))                       # compute all eigenpairs (fine for small L)\nidx = argmin(abs.(E.values))               # index of eigenvalue closest to zero\nv = E.vectors[:, idx]                      # right eigenvector (vec(ρ))\n\n# reshape back to n×n (vec stacks columns, so reshape(v, n, n) gives the matrix)\nρ_ss = reshape(v, 2, 2);\n\n# numeric cleanup: make Hermitian and normalize trace\nρ_ss = (ρ_ss + ρ_ss') / 2  ;               # enforce Hermiticity (conjugate-transpose)\nρ_ss = ρ_ss / tr(ρ_ss) ;                   # normalize trace to 1\n\nWe choose to monitor the electrons entering the cold reservoir \n\n# Vector with weights \nnu = [1];\n\n# Monitored jump operators (photons entering the cold reservoir)\nmJ = [Jcloss];\n\n# Computing the first two cumulants\nc1, c2 = fcscumulants_recursive(L, mJ, 2, sparse(ρ_ss), nu);\nprintln(\"\\nFull Counting Statistics (numerics):\")\nprintln(\"First cumulant : $c1\")\nprintln(\"Second cumulant : $c2\") \n\nFull Counting Statistics (numerics):\nFirst cumulant : 0.08333333333333334\nSecond cumulant : 0.06018518518518519\n\nIn the large bias regime we have the following analytical solutions Patrick P. Potts, 2024\n\nc1_analytical = κc*κh/(κc+κh);\nc2_analytical = (κh^2+κc^2)/(κc+κh)^2*c1_analytical;\nprintln(\"\\nFull Counting Statistics (analytical):\")\nprintln(\"First cumulant : $c1_analytical\")\nprintln(\"Second cumulant : $c2_analytical\") \n\nFull Counting Statistics (analytical):\nFirst cumulant : 0.08333333333333334\nSecond cumulant : 0.0601851851851852","title":"Quantum dot heat engine"},{"category":"section","location":"examples/#Driven-cavity","page":"Overview","text":"We study a simple driven cavity using the QuantumOptics.jl package to model the system\n\nusing QuantumOptics\nusing QuantumFCS\n\nDefine parameters and system \n\n\n# Define the Hilbert space dimension \nN = 10  # Fock space cutoff\n\n# Create the Fock basis for the cavity mode\nbasis = FockBasis(N)\n\n# Define operators\na = destroy(basis)  # annihilation operator\na_dag = create(basis)  # creation operator\nn = number(basis)   # number operator\n\n# System parameters\nω = 1.0     # cavity frequency\nκ = 0.1     # cavity decay rate\nΩ = 0.5     # drive strength (coherent drive)\n\n# Hamiltonian (driven cavity)\nH = ω * n + Ω * (a_dag + a)  # free evolution + coherent drive\n\n# Jump operators (cavity decay)\nJ = [sqrt(κ) * a]  # photon loss\n\n# Vector with weights (for one monitored channel)\nnu = [-1]\n\n# Monitored jump operators (photons leaving the cavity)\nmJ = [sqrt(κ) * a]\n\n# Initial state (vacuum state)\nψ0 = fockstate(basis, 0)\nρ0 = dm(ψ0)\n\nDetermine the steady state \n\nρss = steadystate.iterative(H, J);\n\n\nCompute the first three cumulants\n\nc1, c2, c3 = fcscumulants_recursive(H, J, mJ, 3, ρss, nu)\n\nprintln(\"\\nFull Counting Statistics:\")\nprintln(\"First cumulant (mean photon flux): $c1\")\nprintln(\"Second cumulant (variance): $c2\") \nprintln(\"Third cumulant (skewness): $c3\")\n\nFull Counting Statistics:\nFirst cumulant (mean photon flux): -0.02104572040194588\nSecond cumulant (variance): 0.012890993774510551\nThird cumulant (skewness): 0.011185533737017767","title":"Driven cavity"},{"category":"section","location":"examples/#Applications-from-the-paper","page":"Overview","text":"The two examples above are deliberately small — they exist to show the shape of the API. Two larger applications, taken from the companion paper, are worked through on their own pages:\n\nDriven-dissipative Jaynes–Cummings model — photon-blockade breakdown in a driven cavity coupled to a two-level atom. Three cumulants of the cavity emission, on Liouvillians up to sim10^6times10^6. Shows the iterative backend end to end: the trace-constrained steady state, injecting its incomplete-LU factorization into the Drazin solve, and carrying that factorization (plus a GMRES warm start) across a parameter sweep with adaptive rebuilds.\nCircuit-QED quantum heat engine — photon-assisted Cooper-pair tunnelling between a hot and a cold microwave mode. Two heat currents on one Liouvillian and one steady state, so the Drazin solver is prepared once and reused for both; ends in the thermodynamic-uncertainty-relation analysis those cumulants feed.\n\nBoth pages carry the physics and the numerical-reliability discussion alongside the package usage, and every printed result on them comes from an actual run.\n\nThe full production pipelines behind the manuscript figures — parameter grids, cutoff scheduling, provenance metadata, and the figure routines — live in the companion repository QuantumFCS-Notebooks.","title":"Applications from the paper"},{"category":"section","location":"solvers/#solvers","page":"Drazin solvers","text":"Computing the cumulants of the counting statistics requires applying the (projected) Drazin inverse mathcalL^+ of the Liouvillian to a sequence of right-hand sides — one per cumulant order (see the Mathematical Background for where mathcalL^+ enters the recursion). QuantumFCS never forms mathcalL^+ explicitly; instead it prepares a solver once and applies it to each right-hand side.\n\nTwo backends share a single interface and are selected with the method keyword of fcscumulants_recursive (or the method field of a LindbladFCS problem):\n\nBackend method What it does Best for\nDirect LU :lu (default) Caches one sparse LU factorization of mathcalL and reuses it for every order Small to medium systems; dense Liouvillians\nIterative :iterative Matrix-free preconditioned GMRES (no factorization) Large, sparse Liouvillians","title":"Drazin solvers"},{"category":"section","location":"solvers/#The-LU-backend-(:lu)","page":"Drazin solvers","text":"The default. It factorizes the Liouvillian once with a sparse LU and reuses that factorization across all cumulant orders. Each order then costs only a triangular solve, so it is very fast and accurate whenever the factorization fits in memory.\n\nThis is the right choice for the vast majority of problems. Use it unless you have measured LU factorization (or its memory) to be the bottleneck. A dense Liouvillian always uses this path.\n\nnote: Why LU eventually fails to scale\nThe Liouvillian acts on a vectorized density matrix, so for a Hilbert space of dimension d it is a d^2 times d^2 matrix. Even when mathcalL is sparse, its LU factors suffer fill-in — they become far denser than mathcalL itself — and both runtime and memory grow steeply with d. Past a few hundred Hilbert dimensions this becomes the limiting factor.","title":"The LU backend (:lu)"},{"category":"section","location":"solvers/#The-iterative-backend-(:iterative)","page":"Drazin solvers","text":"For large sparse Liouvillians where LU fill-in is prohibitive, the iterative backend solves each system with preconditioned GMRES and never factorizes mathcalL. It combines three ingredients:\n\nMatrix-free gauge fixing. mathcalL is singular (its kernel is the steady state). We solve with the operator A x = mathcalL x + rho(v_mathbb1cdot x), a rank-1 update that lifts the null mode so A is nonsingular. A is applied as an operator only — it is never assembled, so no fill-in is created.\nShifted incomplete-LU preconditioner. An incomplete LU of mathcalL - sigma I is built once and reused for every order. The shift sigma keeps the incomplete factorization well-behaved so the GMRES iteration count is governed by the preconditioner rather than the (possibly tiny) physical Liouvillian gap.\nReused Krylov workspace. The GMRES basis is allocated once and reused for every cumulant order.\n\nThe result keeps a roughly constant iteration count and bounded memory as d grows, so its advantage over LU widens with system size.","title":"The iterative backend (:iterative)"},{"category":"section","location":"solvers/#Enabling-it","page":"Drazin solvers","text":"The iterative backend lives in a package extension and loads only when its dependencies are present:\n\nusing Krylov, IncompleteLU   # activates QuantumFCSIterativeExt\nusing QuantumFCS\n\ncumulants = fcscumulants_recursive(L, mJ, nC, ρss, nu; method = :iterative)\n\nWithout Krylov and IncompleteLU loaded, method = :iterative raises an informative error. Both are weak dependencies, so they add no load-time cost when you stick to the default backend.","title":"Enabling it"},{"category":"section","location":"solvers/#Tuning-and-caveats","page":"Drazin solvers","text":"The iterative backend exposes a few knobs (keywords of fcscumulants_recursive / fields of LindbladFCS). The defaults are sensible; reach for these only when convergence or performance needs help:\n\nOption Meaning Guidance\nτ ILU drop tolerance Smaller ⇒ denser preconditioner (fewer GMRES iterations, more memory); larger ⇒ sparser/cheaper but may need more iterations. Ignored when Pl is supplied.\nσ Diagonal shift for the preconditioner only nothing auto-scales from mathcalL. Increase if the ILU is unstable. Does not change the solution, only the preconditioner. Ignored when Pl is supplied.\nPl Externally built preconditioner to reuse Skips the internal ILU build; see Reusing a preconditioner below.\nrtol GMRES relative tolerance Loosen for speed, tighten for accuracy.\nitmax GMRES iteration cap Raise if you see a non-convergence warning.\nmemory GMRES restart (Krylov basis) size Larger can improve convergence at higher memory cost.","title":"Tuning and caveats"},{"category":"section","location":"solvers/#reuse-precond","page":"Drazin solvers","text":"Building the ILU is the dominant cost of the iterative backend, and it is often redundant: the steady state ρss is usually obtained by a preconditioned linear solve of the same Liouvillian, so a suitable ILU already exists by the time the cumulants are computed. Pass it as Pl to skip the internal build:\n\nusing Krylov, IncompleteLU\n\n# `Pss` is an ILU of the (shifted) Liouvillian, e.g. the one built for the\n# steady-state solve at this or a neighbouring parameter point.\ncumulants = fcscumulants_recursive(L, mJ, nC, ρss, nu;\n                                   method = :iterative, Pl = Pss)\n\nPl must support LinearAlgebra.ldiv!(y, Pl, x) and ldiv!(Pl, x) — an IncompleteLU.ILUFactorization does — and need only approximate mathcalL^-1 up to a diagonal shift, the rank-1 gauge term, and mild parameter drift. Because an injected preconditioner may have been built for a neighbouring operator, it is applied on the right, so GMRES converges on the true residual rather than a preconditioned surrogate: a genuinely poor Pl shows up as extra iterations or a non-convergence warning, never as a silently wrong result. When Pl is supplied, σ and τ are ignored.\n\nThis is the recommended pattern for parameter sweeps — build (or reuse) one ILU per neighbourhood of points and feed it to both the steady-state solve and the FCS cumulants. The driven-dissipative Jaynes–Cummings example works this through on a real sweep, including when to stop reusing a factorization and rebuild it.\n\ntip: Steady-state accuracy dominates the high cumulants\nBoth backends inherit the accuracy of the steady state ρss you pass in. High cumulants are sensitive to it: an approximate steady state limits agreement between :lu and :iterative at the same order. For tight comparisons use an exact steady state (e.g. steadystate.eigenvector).\n\nwarning: Match the backend to the problem\n:iterative carries preconditioner-setup and per-solve iteration overhead that direct LU does not. On small or dense systems it is slower than :lu. Only switch when LU fill-in is the measured bottleneck.\n\nSee Krylov.jl and IncompleteLU.jl for details on the underlying GMRES and ILU implementations.","title":"Reusing a preconditioner"},{"category":"section","location":"solvers/#steady-state-prep","page":"Drazin solvers","text":"The Reusing a preconditioner pattern above assumes you already have a suitable ILU in hand. trace_constrained_steadystate is the package-level helper that produces one: it solves for the steady state and returns the preconditioner it built, so the two feed straight into the FCS cumulants without a second ILU build.\n\nThe Liouvillian mathcalL is singular (its kernel is the steady state), so the steady state is found from the trace-constrained system Avecrho = b, where the redundant first equation is replaced by the normalization mathrmtrrho = 1. trace_constrained_steadystate builds that system, builds a shifted-ILU preconditioner, solves it with GMRES, and returns a lean TraceConstrainedSteadyState:\n\nusing Krylov, IncompleteLU   # for method = :iterative\nusing QuantumFCS\n\nss = trace_constrained_steadystate(L; method = :iterative)   # solves for ρss and Pl\n\nctx = prepare_fcs_context(ss; method = :iterative)           # reuses ss.Pl — no rebuild\nhot  = fcscumulants_recursive(ctx; mJ = mJ_hot,  nu = nu_hot,  nC = 2)\ncold = fcscumulants_recursive(ctx; mJ = mJ_cold, nu = nu_cold, nC = 2)\n\nss.rho_ss is a hermitianized, trace-normalized sparse matrix; ss.Pl is the steady-state preconditioner; and ss.stats carries scalar diagnostics (convergence, iterations, residuals, trace/hermiticity errors, ILU/GMRES timings). The steady-state Pl was built for the trace-constrained matrix rather than mathcalL itself, so the FCS backend applies it on the right (as with any injected Pl), keeping the GMRES stopping test on the true residual.\n\nmethod = :lu gives a direct-solve baseline (A \\ b, Pl = nothing) for small and medium systems; method = :iterative requires the QuantumFCSIterativeExt extension. Both accept a prebuilt Liouvillian L, a Hamiltonian/jumps pair H, J, or a prebuilt TraceConstrainedSystem — the last enables warm-started continuation across a parameter sweep by reusing one system and preconditioner (Pl, u0) over neighbouring points.\n\nBoth routes are demonstrated in the examples: the Jaynes–Cummings page drives the TraceConstrainedSystem + Pl/u0 continuation directly, and the heat-engine page takes the shorter path of handing the TraceConstrainedSteadyState to prepare_fcs_context.","title":"Preparing the steady state for iterative FCS"},{"category":"section","location":"solvers/#prepared-context","page":"Drazin solvers","text":"The Drazin solver depends only on the Liouvillian mathcalL and the steady state rho_textss — never on the monitored jumps or their weights, which enter only the counting-field super-operators and the recursion's right-hand sides. So when several observables share the same mathcalL and rho_textss — hot and cold heat currents, left and right particle currents, several output channels of a multi-terminal device, or different weight choices for one jump set — the expensive solver preparation (the sparse LU factorization, or the shifted-ILU build) need only be done once.\n\nA plain LindbladFCS problem prepares its solver internally for a single observable, so evaluating k observables that way repeats that preparation k times. prepare_fcs_context instead builds the invariant data once, into a PreparedLindbladFCS, and reuses it for every observable:\n\nctx = prepare_fcs_context(; L = L, rho_ss = ρss, method = :lu)   # prepares the solver once\n\nhot  = fcscumulants_recursive(ctx; mJ = mJ_hot,  nu = nu_hot,  nC = 2)\ncold = fcscumulants_recursive(ctx; mJ = mJ_cold, nu = nu_cold, nC = 2)\n\nThe context accepts the same inputs as LindbladFCS: pass a prebuilt Liouvillian L, or a Hamiltonian H and jump operators J; L/H/J/rho_ss may be plain arrays or backend operators (QuantumOptics, QuantumToolbox). It is backend-generic — the same method, σ, τ, Pl, rtol, itmax, and memory keywords select and tune the :lu or :iterative solve, and configure the one-time preparation:\n\nusing Krylov, IncompleteLU   # for method = :iterative\n\nctx = prepare_fcs_context(; H = H, J = J, rho_ss = ρss, method = :iterative, Pl = Pss)\n\nThe result is numerically identical to preparing the solver anew for each observable (the reuse changes only the cost, never the answer). This is the recommended setup whenever many monitored observables are computed on one Liouvillian and steady state. The circuit-QED heat-engine example uses it for the hot and cold heat currents of a two-terminal engine, and checks both against currents computed directly from rho_textss.","title":"Reusing a prepared solver across observables"},{"category":"section","location":"solvers/#API","page":"Drazin solvers","text":"The context object and its constructor for the workflow above, prepare_fcs_context and PreparedLindbladFCS, are documented with the other problem types on the API page.","title":"API"},{"category":"function","location":"solvers/#QuantumFCS.prepare_drazin_solver","page":"Drazin solvers","text":"prepare_drazin_solver(L, ρ, vId; method=:lu, rtol=1e-12, σ=nothing, τ=0.05,\n                      Pl=nothing, itmax=200, memory=30) -> DrazinSolver\n\nBuild a reusable solver for the (projected) Drazin inverse of the singular Liouvillian L, given its vectorized steady state ρ (right null vector) and the vectorized identity / trace functional vId (left null vector).\n\nmethod = :lu (default) caches a sparse LU and reproduces the existing behavior. method = :iterative builds a matrix-free, preconditioned Krylov solver suited to large sparse Liouvillians where direct-LU fill-in is prohibitive; it requires the QuantumFCSIterativeExt extension (using Krylov, IncompleteLU). Keyword arguments σ/τ/Pl/itmax/memory/rtol configure the iterative backend and are ignored by the LU backend.\n\nPl supplies an externally built preconditioner for the :iterative backend to reuse instead of building its own shifted ILU — for example the ILU already computed for the steady-state solve at the same or a neighbouring parameter point. It must support LinearAlgebra.ldiv!(y, Pl, x) and ldiv!(Pl, x) and approximate L⁻¹ up to a diagonal shift, a low-rank gauge term, and mild parameter drift; it is applied on the right so GMRES converges on the true residual. When Pl is supplied, σ and τ are ignored.\n\n\n\n\n\n","title":"QuantumFCS.prepare_drazin_solver"},{"category":"function","location":"solvers/#QuantumFCS.drazin_solve","page":"Drazin solvers","text":"drazin_solve(solver::DrazinSolver, α) -> SparseVector\n\nApply the prepared (projected) Drazin inverse held by solver to the RHS α. Projects α onto range(L), solves, re-imposes the trace-zero gauge, and returns a sparsified result — identical pre/post-processing to drazin_apply.\n\n\n\n\n\n","title":"QuantumFCS.drazin_solve"},{"category":"type","location":"solvers/#QuantumFCS.DrazinSolver","page":"Drazin solvers","text":"DrazinSolver\n\nAbstract supertype for prepared Drazin-inverse solvers. A concrete solver caches whatever per-Liouvillian work (factorization, preconditioner, matrix-free operator) is reused across cumulant orders; apply it with drazin_solve.\n\n\n\n\n\n","title":"QuantumFCS.DrazinSolver"},{"category":"section","location":"math/#math","page":"Mathematical Background","text":"QuantumFCS.jl computes zero-frequency current cumulants for time-independent Lindblad master equations. The system dynamics are written as\n\nfracdrhodt\n= -iHrho + sum_k=1^r mathcalDJ_krho\nequiv mathcalLrho\n\nwith dissipator\n\nmathcalDArho\n= Arho A^dagger - frac12A^dagger Arho\n\nHere, H is the Hamiltonian, J = (J_1,\\ldots,J_r) is the list of jump operators, and L is the Liouvillian superoperator. The recursive construction assumes that mathcalL has a unique stationary state rho_rm ss, normalised by operatornametrrho_rm ss=1.","title":"Mathematical Background"},{"category":"section","location":"math/#Monitored-jumps-and-weights","page":"Mathematical Background","text":"A stochastic current is defined by choosing a list of monitored jumps\n\nmathcalM = (M_1ldotsM_p) qquad M_l in J_1ldotsJ_r\n\nand a matching list of weights\n\nnu = (nu_1ldotsnu_p)\n\nIn the package, these are the inputs mJ and nu. The weights set both the sign convention and the units of the current: they can be dimensionless values such as pm 1 for particle or photon counts, charges for electric currents, or energy quanta for heat currents.\n\nLet N_l(t) be the number of jumps through monitored channel M_l in the interval 0t. The integrated current is\n\nN(t) = sum_l=1^p nu_l N_l(t)\n= int_0^t dt I(t)\n\nwhich implicitly defines the stochastic current I(t).","title":"Monitored jumps and weights"},{"category":"section","location":"math/#Tilted-Liouvillian","page":"Mathematical Background","text":"To generate the statistics of N(t), introduce the counting-field-resolved density matrix\n\nrho_chi(t)\n= sum_ninmathcalN e^ichi nrho(nt)\n\nwhere operatornametrrho(nt) is the probability that the integrated current has value n. It evolves under the tilted Liouvillian\n\nmathcalL_chi rho_chi\n= mathcalLrho_chi + deltamathcalL_chirho_chi\n\nwith\n\ndeltamathcalL_chirho\n= sum_l=1^p left(e^ichinu_l-1right)M_lrho M_l^dagger\n\nThis sign convention matches the definition of rho_chi above and the package implementation. The integrated-current cumulant generating function is\n\nC_N(chit)=logoperatornametrrho_chi(t)\n\nand the long-time current cumulants are\n\nc_n equiv langlelangle I^nranglerangle\n= left(-ipartial_chi)^n\nlim_ttoinftyfracC_N(chit)tright_chi=0","title":"Tilted Liouvillian"},{"category":"section","location":"math/#Recursive-cumulant-scheme","page":"Mathematical Background","text":"The package computes these cumulants without differentiating the dominant eigenvalue of mathcalL_chi numerically. In vectorised notation, Aranglerangle denotes the vector form of an operator and langlelangle 1 is the trace functional. Define\n\nmathcalL^(n)\n= left(-ipartial_chi)^n mathcalL_chiright_chi=0\n= sum_l=1^p nu_l^n M_l^* otimes M_l\n\nStarting from sigma_0ranglerangle = rho_rm ssranglerangle, the cumulants and auxiliary pseudo-states are generated recursively as\n\nlanglelangle I^nranglerangle\n= sum_m=1^n nchoose m\nlanglelangle 1mathcalL^(m)sigma_n-mranglerangle\n\nsigma_nranglerangle\n= mathcalL^+\nsum_m=1^n nchoose m\nleft(langlelangle I^mrangleranglemathbbI\n- mathcalL^(m)right)\nsigma_n-mranglerangle\n\nHere, mathcalL^+ is the Drazin inverse of the Liouvillian on the subspace orthogonal to the stationary state. Numerically, QuantumFCS.jl prepares a solver for applying mathcalL^+ and reuses that solver across cumulant orders; see Drazin solvers.","title":"Recursive cumulant scheme"},{"category":"section","location":"math/#Mapping-to-package-inputs","page":"Mathematical Background","text":"The mathematical objects above map directly to the main package interface:\n\n(H, J) or a preassembled L define the Liouvillian mathcalL.\nrho_ss supplies the stationary state rho_rm ss.\nmJ is the monitored-jump list mathcalM.\nnu is the weight list nu.\nnC is the number of cumulants to return.\n\nThese inputs can be passed directly to fcscumulants_recursive, or bundled in a LindbladFCS problem.","title":"Mapping to package inputs"},{"category":"section","location":"math/#References","page":"Mathematical Background","text":"FCS in Lindblad master equations: Potts (2024). \"Quantum Thermodynamics\" arXiv\nFCS, recursive scheme, vectorization, Drazin inverse: Landi et al. (2024). \"Current fluctuations in open quantum systems: Bridging the gap between quantum continuous measurements and full counting statistics\", PRX Quantum 5, 020201 (2024) arXiv \nDetailed exposition of the recursive scheme: Flindt et al. (2010). \"Counting statistics of transport through Coulomb blockade nanostructures: High-order cumulants and non-Markovian effects\" Phys. Rev. B 82, 155407 arXiv","title":"References"},{"category":"section","location":"#QuantumFCS.jl","page":"Home","text":"QuantumFCS.jl computes the Full Counting Statistics (FCS) of open quantum systems — current cumulants to arbitrary order for Lindblad master equations — using a recursive scheme built on the Drazin inverse of the Liouvillian. Currents are specified by choosing monitored jump operators (mJ) and their weights (nu), so the same Liouvillian can be reused for particle, photon, charge, or heat currents.\n\nThe package works with plain arrays, QuantumOptics.jl, and QuantumToolbox.jl. It ships two Drazin backends (direct LU and a matrix-free iterative solver), plus prepared contexts for reusing solver setup across several observables.\n\n⚡ Start with the Quickstart\n📘 Read up on the Mathematical Background\n⚙️ Pick a backend for large systems in Drazin solvers\n📝 Study the Examples, including two applications from the companion paper: the driven-dissipative Jaynes–Cummings model and a circuit-QED heat engine\n🧭 Browse the API","title":"QuantumFCS.jl"},{"category":"section","location":"examples/jaynes_cummings/#jc-example","page":"Driven-dissipative Jaynes-Cummings","text":"A coherently driven cavity coupled to a two-level atom, with cavity loss only. In the frame rotating at the drive frequency,\n\nH = -Delta(a^dagger a + sigma_+sigma_-)\n    + g(a^daggersigma_- + asigma_+)\n    - mathcalE(a + a^dagger)\nqquad\ndotrho = -iHrho + kappamathcalDarho \n\nwith Delta the common cavity/atom detuning from the drive, g the Jaynes–Cummings coupling, mathcalE the drive amplitude, and kappa the cavity linewidth (which sets the unit of energy throughout).\n\nWe count the cavity emission kappaarho a^dagger — a single monitored jump with weight +1 — and compute the first three cumulants of the emitted-photon counting statistics:\n\nfracc_1kappa = langle a^dagger arangle\nqquad\nmathcalF = fracc_2c_1\nqquad\nmathcalG = fracc_3c_2^32 \n\nthe count rate, the Fano factor, and the skewness. Sweeping the dimensionless drive x = 2mathcalEg at fixed gkappa = 14 crosses the photon-blockade breakdown. On the detuned cuts the cavity becomes bimodal — a dim, blockaded state coexisting with a bright, lasing-like one — and the emission is strongly intermittent, which the higher cumulants register far more sharply than the mean does.\n\nnote: Why this problem needs the iterative backend\nThe bright branch carries hundreds of photons, so the Fock cutoff must grow with the drive. At N = 500 the Liouvillian is a sim10^6 times 10^6 sparse non-Hermitian matrix: forming an LU factorization of it is hopeless, and every point needs a steady state and three Drazin solves. This page is a worked example of the iterative backend — in particular of building one incomplete-LU factorization per parameter neighbourhood and using it for everything.\n\nThis page is a self-contained condensation of the companion notebook driven_dissipative_jaynes_cummings.ipynb, which reproduces Fig. 2 of the manuscript.","title":"Driven-dissipative Jaynes–Cummings model"},{"category":"section","location":"examples/jaynes_cummings/#Setup","page":"Driven-dissipative Jaynes-Cummings","text":"using QuantumToolbox\nusing QuantumFCS\nusing Krylov, IncompleteLU          # enables the iterative backend\nusing SparseArrays, LinearAlgebra, Printf\n\nThe operators are built once for a given Fock cutoff N. The two Hamiltonian pieces that a sweep rescales — the Jaynes–Cummings coupling and the drive — are returned separately, so a sweep can rebuild H cheaply while the operators stay fixed.\n\nconst g = 14.0     # Jaynes-Cummings coupling, in units of κ\nconst κ = 1.0      # cavity linewidth sets the unit\n\n# Cavity + atom operators at Fock cutoff N (Hilbert dimension 2(N+1)).\nfunction jc_operators(N)\n    a  = tensor(destroy(N + 1), qeye(2))\n    sm = tensor(qeye(N + 1), sigmam())\n    return (; a, sm,\n              Hjc         = a' * sm + a * sm',      # a†σ₋ + aσ₊\n              Hdr         = -(a + a'),              # -(a + a†)\n              n_op        = a' * a,\n              detuning_op = a' * a + sm' * sm)\nend\n\n# The Hamiltonian at one (Δ, E) point is a linear combination of those pieces.\njc_hamiltonian(ops, Δ, g, E) = -Δ * ops.detuning_op + g * ops.Hjc + E * ops.Hdr\n\nN_demo = 150\nops = jc_operators(N_demo)\ncavity_loss = sqrt(κ) * ops.a      # the single jump operator, and the counted channel\n\n@printf(\"cutoff N = %d  ->  Hilbert dim = %d, Liouvillian %d x %d\\n\",\n        N_demo, 2 * (N_demo + 1), (2 * (N_demo + 1))^2, (2 * (N_demo + 1))^2)\n\ncutoff N = 150  ->  Hilbert dim = 302, Liouvillian 91204 x 91204","title":"Setup"},{"category":"section","location":"examples/jaynes_cummings/#One-point,-start-to-finish","page":"Driven-dissipative Jaynes-Cummings","text":"This is the whole QuantumFCS call sequence for a single drive point.\n\ntrace_constrained_system replaces the singular condition mathcalLrho = 0 with a non-singular linear system by imposing operatornametrrho = 1.\ntrace_constrained_steadystate solves it with GMRES preconditioned by a shifted incomplete-LU factorization, and returns that factorization in ss.Pl.\nLindbladFCS with Pl = ss.Pl hands the same factorization to the Drazin-inverse solves inside the cumulant recursion.\n\nBuilding the ILU is a near-complete factorization and dominates the cost of a point, so step 3 is what makes the point cost one factorization rather than two. See Reusing a preconditioner for the general rule.\n\nThe counting data is the monitored jump list mJ = [√κ a] with weight nu = [1.0]; nC = 3 asks for the first three cumulants.\n\nΔ_demo, x_demo = 0.55, 0.60\nE_demo = x_demo * g / 2\n\nH   = jc_hamiltonian(ops, Δ_demo, g, E_demo)\nL   = liouvillian(H, [cavity_loss])\nsys = trace_constrained_system(L)\n\n# --- steady state (also produces the preconditioner we reuse below) ----------\nss = trace_constrained_steadystate(sys;\n    method       = :iterative,\n    τ            = 1e-1,      # ILU drop tolerance\n    shift_factor = 1e-6,      # diagonal shift keeping the factorization stable\n    rtol = 1e-10, atol = 1e-14, itmax = 120, memory = 60)\n\nρss  = QuantumObject(ss.rho_ss; type = Operator, dims = L.dimensions)\nn_ss = real(expect(ops.n_op, ρss))\n\n# --- cumulants, reusing the steady-state ILU as the Drazin preconditioner ----\ncumulants = fcscumulants_recursive(LindbladFCS(\n    L      = SparseMatrixCSC{ComplexF64,Int}(sys.L),\n    mJ     = [cavity_loss],   # monitored jump: cavity emission\n    rho_ss = ρss,\n    nu     = [1.0],           # counting weight\n    nC     = 3,\n    method = :iterative,\n    Pl     = ss.Pl,           # <-- the factorization built for the steady state\n    rtol = 1e-8, itmax = 300, memory = 60))\n\nc1, c2, c3 = cumulants\n@printf(\"Δ/κ = %.2f, x = 2E/g = %.2f, N = %d\\n\", Δ_demo, x_demo, N_demo)\n@printf(\"  ⟨a†a⟩   = %.10g\\n\", n_ss)\n@printf(\"  c₁      = %.10g      Fano  c₂/c₁       = %.6g\\n\", c1, c2 / c1)\n@printf(\"  c₂      = %.10g      skew  c₃/c₂^{3/2} = %.6g\\n\", c2, c3 / c2^1.5)\n@printf(\"  steady state converged: %s in %d iterations\\n\",\n        ss.stats.converged, ss.stats.iterations)\n@printf(\"  identity |c₁/(κ⟨a†a⟩) − 1| = %.3e\\n\", abs(c1 / (κ * n_ss) - 1))\n\nΔ/κ = 0.55, x = 2E/g = 0.60, N = 150\n  ⟨a†a⟩   = 0.1085414737\n  c₁      = 0.1085414737      Fano  c₂/c₁       = 22.9967\n  c₂      = 2.496090669      skew  c₃/c₂^{3/2} = 45.9077\n  steady state converged: true in 11 iterations\n  identity |c₁/(κ⟨a†a⟩) − 1| = 0.000e+00\n\nThe last line is an exact identity, not a tolerance: for a single monitored loss channel of weight +1, the first cumulant of the counting statistics is the photon loss rate,\n\nc_1 = kappalangle a^dagger arangle \n\nIt couples the FCS result to a quantity computed independently from rho_textss, so it detects a mis-solved Drazin system, a mis-specified weight, and a non-converged steady state alike. It is the acceptance gate applied at every point of the sweeps below.","title":"One point, start to finish"},{"category":"section","location":"examples/jaynes_cummings/#Choosing-the-Fock-cutoff","page":"Driven-dissipative Jaynes-Cummings","text":"A single cutoff for the whole sweep would be wasteful at low drive and wrong at high drive. Instead each point gets its own cutoff, chosen before solving from a semiclassical estimate n of the bright-branch photon number and padded for the coherent-state width:\n\nN_texthard = n + textttpad_sigmasqrtn + textttpad_abs\nqquad\nN_texttarget = maxleft(N_texthard ntextttocc_maxright)\n\nrounded up to the next tier of a fixed ladder. The sqrtn term is the coherent-state truncation criterion: a cutoff at or above N_texthard resolves the mean, the Fano factor, and the boundary tail. The extra ntextttocc_max headroom exists purely for c_3, which — as the convergence table below shows — is roughly two orders of magnitude more truncation-sensitive than c_1.\n\nOn resonance the estimate is closed-form,\n\nn(Delta = 0) = fracmaxleft(4mathcalE^2 - g^2 0right)kappa^2\n\nwhich is zero below the breakdown drive x = 1: there is no bright branch to accommodate, and the absolute floor pad_abs sets the cutoff. Off resonance the bright branch is the largest root of the neoclassical state equation ukappa_C^2 + D_s(u)^2 = mathcalE^2 with the effective detuning D_s(u) = Delta - smathrmsgn(Delta)g^2sqrtDelta^2 + 4g^2u over both branches s = pm1; the companion repository's jc_model.jl solves it by bisection.\n\nconst TIERS   = (150, 175, 200, 225, 250, 275, 300, 350, 400, 450, 500)\nconst OCC_MAX = 0.50\nconst PAD_ABS = 25.0\n\n# Semiclassical bright-branch photon number, resonant cut (Δ = 0).\njc_bright_n(g, E; κ = 1.0) = max(4 * abs2(E) - abs2(g), 0.0) / abs2(κ)\n\n# Smallest tier meeting both the coherent-width requirement and the occupation\n# aspiration; `clamped` marks a point the ladder cannot resolve at all.\nfunction jc_cutoff(n_est; pad_sigma = 6.0, pad_abs = PAD_ABS, occ_max = OCC_MAX)\n    n      = max(n_est, 0.0)\n    hard   = n + pad_sigma * sqrt(n) + pad_abs\n    target = max(hard, n / occ_max)\n    idx    = findfirst(≥(target), TIERS)\n    return (N = idx === nothing ? last(TIERS) : TIERS[idx],\n            hard = hard, target = target, clamped = hard > last(TIERS))\nend\n\nfunction jc_cutoff_table(xs; pad_sigma)\n    println(\"     x      n_est     N_hard   N_target      N   headroom (N−n)/√n\")\n    for x in xs\n        n = jc_bright_n(g, x * g / 2; κ = κ)\n        c = jc_cutoff(n; pad_sigma = pad_sigma)\n        @printf(\"%6.2f %10.3f %10.1f %10.1f %6d %18s\\n\",\n                x, n, c.hard, c.target, c.N,\n                n > 0 ? @sprintf(\"%.1f\", (c.N - n) / sqrt(n)) : \"— (no bright branch)\")\n    end\nend\n\nprintln(\"Resonant cut (Δ = 0), pad_sigma = 14:\")\njc_cutoff_table((0.30, 0.70, 1.00, 1.10, 1.25, 1.45); pad_sigma = 14.0)\n\nResonant cut (Δ = 0), pad_sigma = 14:\n     x      n_est     N_hard   N_target      N   headroom (N−n)/√n\n  0.30      0.000       25.0       25.0    150 — (no bright branch)\n  0.70      0.000       25.0       25.0    150 — (no bright branch)\n  1.00      0.000       25.0       25.0    150 — (no bright branch)\n  1.10     41.160      156.0      156.0    175               20.9\n  1.25    110.250      282.2      282.2    300               18.1\n  1.45    216.090      446.9      446.9    450               15.9\n\nTwo things are worth noting about this schedule. First, points sharing a cutoff form a segment, and a segment is exactly the span over which one ILU factorization gets reused — the ladder is therefore a performance decision as much as an accuracy one. Second, pad_sigma is tuned per detuning cut in the manuscript run: the resonant cut carries small photon numbers across most of the drive range, and the sigma-headroom (N-n)sqrtn that bounds the c_3 truncation bias is smallest precisely where n is small. Giving the resonant cut pad_sigma = 14 rather than the default 6 lifts it to sim16sigma and removes a c_3 artefact near x approx 125, while leaving the detuned cuts — already well resolved at their higher N — untouched.","title":"Choosing the Fock cutoff"},{"category":"section","location":"examples/jaynes_cummings/#The-sweep","page":"Driven-dissipative Jaynes-Cummings","text":"Three mechanisms make a long sweep affordable, and all three are visible in the loop below:\n\npreconditioner reuse — the shifted ILU is built for the first point of a segment and passed back in as Pl for every later point. Building it dominates the cost of a point, so this is the difference between minutes and hours.\nwarm starting — each solve starts GMRES from the previous point's steady state via u0, since neighbouring drive points have similar states.\nadaptive rebuild — as the drive moves the state, a reused factorization eventually goes stale. The tell is the iteration count creeping up, or GMRES failing outright, and that is precisely when we rebuild. Reuse while it works; rebuild only when it stops working.\n\nThe same Pl is then injected into the cumulant solve, so a point costs one factorization in total.\n\ntip: Wrap the loop in a function\nThe preconditioner and the warm start are carried in local variables across iterations. At top level those would be globals, which under Julia's soft scope rules is both type-unstable and markedly slower. Putting the loop in a function also makes a segment re-runnable at will.\n\nfunction jc_drive_segment(Δ, x_values, N; g, κ = 1.0,\n                          ilu_tau = 1e-1, shift_factor = 1e-6, nC = 3,\n                          itmax = 120, fallback_itmax = 300, rebuild_niter = 80)\n    ops  = jc_operators(N)\n    loss = sqrt(κ) * ops.a\n\n    Pl     = nothing    # segment preconditioner: built on the first point, then reused\n    u_prev = nothing    # warm start carried from the previous drive point\n    rows   = NamedTuple[]\n\n    for x in x_values\n        E  = x * g / 2\n        t0 = time_ns()\n\n        H   = jc_hamiltonian(ops, Δ, g, E)\n        L   = liouvillian(H, [loss])\n        sys = trace_constrained_system(L)\n\n        ss = trace_constrained_steadystate(sys;\n            method       = :iterative,\n            Pl           = Pl,        # reuse the segment factorization (nothing ⇒ build it)\n            u0           = u_prev,    # warm start from the previous point\n            τ            = ilu_tau,\n            shift_factor = shift_factor,\n            rtol = 1e-10, atol = 1e-14, itmax = itmax, memory = 60)\n\n        # As the drive moves the state, a reused factorization eventually goes\n        # stale: GMRES starts needing many iterations, or stops converging. That\n        # is the signal to rebuild it — and only then.\n        rebuilt = false\n        if !ss.stats.converged || ss.stats.iterations > rebuild_niter\n            ss = trace_constrained_steadystate(sys;\n                method       = :iterative,\n                Pl           = nothing,                   # force a fresh factorization\n                u0           = vec(Matrix(ss.rho_ss)),\n                τ            = ilu_tau,\n                shift_factor = shift_factor,\n                rtol = 1e-10, atol = 1e-14, itmax = fallback_itmax, memory = 60)\n            rebuilt = true\n        end\n\n        Pl         = ss.Pl                       # keep it for the next point\n        u_prev     = vec(Matrix(ss.rho_ss))\n        ss_seconds = (time_ns() - t0) / 1e9\n\n        ρss = QuantumObject(ss.rho_ss; type = Operator, dims = L.dimensions)\n        n   = real(expect(ops.n_op, ρss))\n\n        t1 = time_ns()\n        c = fcscumulants_recursive(LindbladFCS(\n            L = SparseMatrixCSC{ComplexF64,Int}(sys.L),\n            mJ = [loss], rho_ss = ρss, nu = [1.0], nC = nC,\n            method = :iterative, Pl = Pl,        # same factorization drives the Drazin solve\n            rtol = 1e-8, itmax = 300, memory = 60))\n        fcs_seconds = (time_ns() - t1) / 1e9\n\n        current_check = c[1] / (κ * n)\n        push!(rows, (; Δ, x, N, n, c1 = c[1], c2 = c[2], c3 = c[3],\n                       Fano = c[2] / c[1], current_check, rebuilt,\n                       ss_iterations = ss.stats.iterations, ss_seconds, fcs_seconds))\n\n        @printf(\"Δ=%.2f x=%.2f N=%3d | n=%10.4g  F=%8.4g | c₁/(κn)−1=%8.1e | ss %5.2fs  fcs %5.2fs%s\\n\",\n                Δ, x, N, n, c[2] / c[1], current_check - 1, ss_seconds, fcs_seconds,\n                rebuilt ? \"  [ILU rebuilt]\" : \"\")\n        flush(stdout)\n    end\n    return rows\nend\n\nRun it on the resonant cut, where the whole low-drive range shares one cutoff — a genuine segment of the production sweep, in about a minute:\n\nx_sweep = collect(0.05:0.05:0.70)\nN_sweep = maximum(jc_cutoff(jc_bright_n(g, x * g / 2; κ = κ); pad_sigma = 14.0).N\n                  for x in x_sweep)\n\nsegment = jc_drive_segment(0.0, x_sweep, N_sweep; g = g, κ = κ)\n\n@printf(\"\\ntotal: %d points, %.1f s  |  worst |c₁/(κn)−1| = %.2e\\n\",\n        length(segment),\n        sum(r.ss_seconds for r in segment) + sum(r.fcs_seconds for r in segment),\n        maximum(abs(r.current_check - 1) for r in segment))\n\nΔ=0.00 x=0.05 N=150 | n= 7.881e-07  F=   2.023 | c₁/(κn)−1=-2.2e-16 | ss  1.24s  fcs  0.78s\nΔ=0.00 x=0.10 N=150 | n= 1.299e-05  F=    2.09 | c₁/(κn)−1=-4.4e-16 | ss  0.61s  fcs  0.74s\nΔ=0.00 x=0.15 N=150 | n= 6.909e-05  F=   2.211 | c₁/(κn)−1= 0.0e+00 | ss  0.99s  fcs  0.74s\nΔ=0.00 x=0.20 N=150 | n= 0.0002343  F=   2.396 | c₁/(κn)−1= 2.2e-16 | ss  1.49s  fcs  1.39s\nΔ=0.00 x=0.25 N=150 | n= 0.0006273  F=   2.663 | c₁/(κn)−1= 0.0e+00 | ss  3.08s  fcs  1.54s\nΔ=0.00 x=0.30 N=150 | n=   0.00146  F=   3.038 | c₁/(κn)−1=-5.6e-16 | ss  1.93s  fcs  2.71s\nΔ=0.00 x=0.35 N=150 | n=  0.003108  F=   3.563 | c₁/(κn)−1=-4.4e-16 | ss  3.13s  fcs  4.24s\nΔ=0.00 x=0.40 N=150 | n=  0.006254  F=   4.295 | c₁/(κn)−1=-1.1e-16 | ss  2.44s  fcs  5.90s\nΔ=0.00 x=0.45 N=150 | n=   0.01214  F=   5.321 | c₁/(κn)−1= 2.2e-16 | ss  7.08s  fcs  0.97s  [ILU rebuilt]\nΔ=0.00 x=0.50 N=150 | n=   0.02307  F=   6.764 | c₁/(κn)−1= 0.0e+00 | ss  1.73s  fcs  1.08s\nΔ=0.00 x=0.55 N=150 | n=    0.0434  F=   8.802 | c₁/(κn)−1= 0.0e+00 | ss  1.69s  fcs  2.80s\nΔ=0.00 x=0.60 N=150 | n=   0.08138  F=   11.67 | c₁/(κn)−1=-8.9e-16 | ss  1.73s  fcs  2.58s\nΔ=0.00 x=0.65 N=150 | n=    0.1529  F=   15.66 | c₁/(κn)−1= 0.0e+00 | ss  2.39s  fcs  5.38s\nΔ=0.00 x=0.70 N=150 | n=    0.2888  F=   21.09 | c₁/(κn)−1=-1.8e-15 | ss  3.49s  fcs  7.18s\n\ntotal: 14 points, 71.1 s  |  worst |c₁/(κn)−1| = 1.78e-15\n\nThe identity holds to roundoff at every point. The steady-state time drops after the first point — that one paid for the factorization — and rises again only at x = 0.45, where the reuse heuristic decided the factorization had drifted too far and rebuilt it. Note also that the Fano factor is already at mathcalF approx 21 by the end of this range: emission on the approach to breakdown is enormously super-Poissonian, and this is the low-drive tail, not the transition.","title":"The sweep"},{"category":"section","location":"examples/jaynes_cummings/#Numerical-reliability:-why-the-boundary-tail-is-not-enough","page":"Driven-dissipative Jaynes-Cummings","text":"The conventional truncation diagnostic is the boundary population p_N-1 + p_N of the cavity Fock distribution. In the coexistence region it is actively misleading: the truncated ladder simply cannot host the bright state, so the solver locks onto the dim branch, converges beautifully, and leaves no trace in the boundary population.\n\nThe honest check is cross-cutoff agreement of the cumulants. Recomputing one point on a ladder of cutoffs:\n\nfunction jc_cutoff_convergence(Δ, x, cutoffs; g, κ = 1.0)\n    rel(new, old) = old === nothing ? NaN : abs(new / old - 1)\n    prev = nothing\n    println(\"    N            c₁            c₂            c₃   Δc₁/c₁   Δc₂/c₂   Δc₃/c₃  p_{N-1}+p_N\")\n    for N in cutoffs\n        ops = jc_operators(N)\n        H   = jc_hamiltonian(ops, Δ, g, x * g / 2)\n        L   = liouvillian(H, [sqrt(κ) * ops.a])\n        sys = trace_constrained_system(L)\n\n        ss = trace_constrained_steadystate(sys;\n            method = :iterative, τ = 1e-1, shift_factor = 1e-6,\n            rtol = 1e-10, atol = 1e-14, itmax = 300, memory = 60)\n        ρ = QuantumObject(ss.rho_ss; type = Operator, dims = L.dimensions)\n\n        c = fcscumulants_recursive(LindbladFCS(\n            L = SparseMatrixCSC{ComplexF64,Int}(sys.L),\n            mJ = [sqrt(κ) * ops.a], rho_ss = ρ, nu = [1.0], nC = 3,\n            method = :iterative, Pl = ss.Pl,\n            rtol = 1e-8, itmax = 300, memory = 60))\n\n        pn = max.(real.(diag(ptrace(ρ, 1).data)), 0.0);  pn ./= sum(pn)\n\n        @printf(\"%5d %13.8g %13.8g %13.8g %8.1e %8.1e %8.1e %12.2e\\n\",\n                N, c[1], c[2], c[3],\n                rel(c[1], prev === nothing ? nothing : prev[1]),\n                rel(c[2], prev === nothing ? nothing : prev[2]),\n                rel(c[3], prev === nothing ? nothing : prev[3]),\n                sum(pn[end-1:end]))\n        prev = c\n    end\nend\n\njc_cutoff_convergence(0.55, 0.60, (100, 150, 200, 250); g = g, κ = κ)\n\n    N            c₁            c₂            c₃   Δc₁/c₁   Δc₂/c₂   Δc₃/c₃  p_{N-1}+p_N\n  100    0.10854147     2.4960861     181.03752      NaN      NaN      NaN     0.00e+00\n  150    0.10854147     2.4960907     181.04067  6.0e-08  1.8e-06  1.7e-05     0.00e+00\n  200    0.10854148     2.4960932     181.04257  3.5e-08  1.0e-06  1.1e-05     1.11e-44\n  250    0.10854147     2.4960911     181.04105  2.8e-08  8.4e-07  8.4e-06     0.00e+00\n\nTwo things to read off this table.\n\nFirst, the cumulants converge at very different rates. One cutoff step moves c_1 by sim10^-8, c_2 by sim10^-6, and c_3 by sim10^-5 — roughly two orders of magnitude between the first and third cumulant. That ordering is why the scheduler buys headroom aimed at c_3: a cutoff that has clearly converged the count rate has not necessarily converged the skewness.\n\nSecond — and this is the point — the boundary tail is at (or numerically indistinguishable from) zero for every cutoff in the table, while c_3 is still moving. The conventional diagnostic reports \"fully converged\" throughout. Trusting it would mean stopping at the smallest cutoff and quietly carrying a 10^-5 bias in the skewness.","title":"Numerical reliability: why the boundary tail is not enough"},{"category":"section","location":"examples/jaynes_cummings/#The-manuscript-figure","page":"Driven-dissipative Jaynes-Cummings","text":"(Image: Driven-dissipative Jaynes–Cummings full counting statistics at g/κ = 14)\n\nThe published figure runs the sweep above over a graded ~89-point drive grid on three detuning cuts Deltakappa in 0 055 070, with cutoffs climbing to N = 500 — about an hour, and several GB of memory at the deepest points. It shows:\n\n(a–c) the cavity Fock distributions P_n at three drives per cut; the dim/bright bimodality is directly visible on the detuned cuts;\n(d–f) the count rate c_1kappa, the Fano factor mathcalF, and the skewness mathcalG;\n(g–i) the reliability diagnostics recorded per point during the sweep: the cutoff actually used, the truncation tail p_N-1+p_N against varepsilon_texttrunc, and the occupation langle a^dagger arangle  N.\n\nThe full production pipeline — the detuned semiclassical root-finding, the per-cut pad_sigma, the per-point retry on a failed acceptance gate, and the figure routines — lives in the companion repository QuantumFCS-Notebooks.","title":"The manuscript figure"},{"category":"section","location":"examples/jaynes_cummings/#See-also","page":"Driven-dissipative Jaynes-Cummings","text":"Circuit-QED quantum heat engine — the same iterative machinery with several monitored currents sharing one prepared solver.\nDrazin solvers — the backends, their knobs, and the preconditioner-reuse rules used here.","title":"See also"},{"category":"section","location":"examples/circuit_qed_heat_engine/#qhe-example","page":"Circuit-QED heat engine","text":"A voltage-biased Josephson junction couples a hot and a cold microwave mode. Each Cooper pair tunnelling across the junction converts its electrostatic energy 2eV into photons, and tuning the bias to\n\n2eV = l_h Omega_h - l_c Omega_c\n\nselects which photon-exchange process is resonant. This example uses l_h = 1 l_c = 2: one hot photon in, two cold photons out. In the rotating-wave approximation,\n\nH = -fracE_J2left i^l_h+l_c\n    (a_c^dagger)^l_c A_h(l_h) A_c(l_c) a_h^l_h\n    + texthc right\nqquad\nE_J = frac2g(2lambda_h)^l_h(2lambda_c)^l_c\n\nwhere the A_alpha are diagonal Laguerre operators carrying the junction's nonlinear matrix elements and g is the effective photon–photon coupling. Each mode is damped by its own thermal bath, giving four jump operators — emission into and absorption from each bath:\n\nJ = Bigsqrt(bar n_h+1)kappa_ha_hquad\n        sqrt(bar n_c+1)kappa_ca_cquad\n        sqrtbar n_h kappa_ha_h^daggerquad\n        sqrtbar n_c kappa_ca_c^daggerBig\n\nWe monitor two currents — the heat flowing into the hot bath and into the cold bath — and compute the first two cumulants of each: the mean currents langle J_alpharangle and their noise langlelangle J_alpha^2ranglerangle. From these follow the Fano factors, the entropy production, and the thermodynamic-uncertainty-relation (TUR) product.\n\nnote: Why this problem is a good fit for prepared contexts\nBoth heat currents live on the same Liouvillian and the same steady state. Only the monitored jumps and their weights differ. The Drazin solver depends on neither, so it can be prepared once and reused — which is exactly what prepare_fcs_context is for. This page is the worked example of the pattern described under Reusing a prepared solver across observables.\n\nThis page is a self-contained condensation of the companion notebook circuit_qed_heat_engine.ipynb, which reproduces Figs. 3 and 4 of the manuscript.","title":"Circuit-QED quantum heat engine"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Setup-and-parameters","page":"Circuit-QED heat engine","text":"using QuantumToolbox\nusing QuantumFCS\nusing Krylov, IncompleteLU          # enables the iterative backend\nusing SparseArrays, LinearAlgebra, Printf\n\nparams = (\n    Nmax_h = 7,   Nmax_c = 7,        # Fock cutoffs: Hilbert dimension (Nmax_h+1)(Nmax_c+1)\n    lh     = 1,   lc     = 2,        # one hot photon in, two cold photons out\n    λh     = 0.47, λc    = 0.89,     # junction coupling parameters\n    Ωc     = 1000.0, Ωratio = π,     # cold-mode frequency and Ω_h/Ω_c\n    κh     = 2.0, κc     = 0.5,      # bath couplings\n    nh     = 0.5, nc     = 0.01,     # thermal occupations\n    g      = 12.841683366733466,     # effective photon-photon coupling\n)\n\nThese are the operating parameters of the large-affinity regime, where the engine turns out to be antibunched.","title":"Setup and parameters"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Building-the-model","page":"Circuit-QED heat engine","text":"The Laguerre operators are diagonal in the Fock basis, with matrix elements\n\nlangle n  A_alpha(k)  n rangle\n= (2lambda)^k e^-2lambda^2 fracn(n+k) L_n^(k)(4lambda^2)\n\nwhere L_n^(k) is a generalized Laguerre polynomial. Evaluating it with the standard three-term recurrence\n\n(n+1)L_n+1^(alpha)(x)\n= (2n+1+alpha-x)L_n^(alpha)(x) - (n+alpha)L_n-1^(alpha)(x)\n\nkeeps this page dependency-free; the companion notebook instead evaluates the same polynomial through the confluent hypergeometric function pFq from HypergeometricFunctions.jl, and the two agree to machine precision. Note also that n(n+k) is written as 1prod_j=n+1^n+k j rather than as a ratio of factorials, which would overflow well before the cutoffs used in the finite-affinity regime.\n\n# Generalized Laguerre polynomial L_n^{(α)}(x), three-term recurrence.\nfunction laguerre(n::Integer, α::Real, x::Real)\n    n == 0 && return 1.0\n    Lm1, L = 1.0, 1 + α - x\n    for k in 1:(n - 1)\n        Lm1, L = L, ((2k + 1 + α - x) * L - (k + α) * Lm1) / (k + 1)\n    end\n    return L\nend\n\n# ⟨n| A_α(k) |n⟩: the junction matrix element for a k-photon process.\nlaguerre_element(n, k, λ) =\n    (2λ)^k * exp(-2λ^2) * laguerre(n, k, 4λ^2) / prod(n+1:n+k)\n\nlaguerre_operator(λ, k, N) = QuantumObject(\n    spdiagm(0 => ComplexF64[laguerre_element(n, k, λ) for n in 0:(N - 1)]);\n    type = Operator, dims = (N,))\n\nfunction qhe_model(p)\n    Nh, Nc = p.Nmax_h + 1, p.Nmax_c + 1\n    ah = tensor(destroy(Nh), qeye(Nc))\n    ac = tensor(qeye(Nh), destroy(Nc))\n\n    Ej = 2 * p.g / ((2p.λh)^p.lh * (2p.λc)^p.lc)\n    A  = tensor(laguerre_operator(p.λh, p.lh, Nh),\n                laguerre_operator(p.λc, p.lc, Nc))\n\n    H = -Ej / 2 * (im^(p.lh + p.lc) * (ac')^p.lc * A * ah^p.lh)\n    H = H + H'\n\n    J = [sqrt((p.nh + 1) * p.κh) * ah,     # emission into the hot bath\n         sqrt((p.nc + 1) * p.κc) * ac,     # emission into the cold bath\n         sqrt(p.nh * p.κh) * ah',          # absorption from the hot bath\n         sqrt(p.nc * p.κc) * ac']          # absorption from the cold bath\n    return (; H, J, ah, ac, Ej, dims = (Nh, Nc))\nend\n\nmodel = qhe_model(params)\nΩh = params.Ωratio * params.Ωc\n\n@printf(\"Hilbert space: %d x %d = %d   (Liouvillian %d x %d)\\n\",\n        params.Nmax_h + 1, params.Nmax_c + 1, prod(model.dims),\n        prod(model.dims)^2, prod(model.dims)^2)\n@printf(\"Ω_h = %.2f, Ω_c = %.2f, resonance 2eV = Ω_h - 2Ω_c = %.2f\\n\",\n        Ωh, params.Ωc, Ωh - 2 * params.Ωc)\n@printf(\"g = %.4f  ->  E_J = %.4f\\n\", params.g, model.Ej)\n@printf(\"%d jump operators (hot/cold emission and absorption)\\n\", length(model.J))\n\nHilbert space: 8 x 8 = 64   (Liouvillian 4096 x 4096)\nΩ_h = 3141.59, Ω_c = 1000.00, resonance 2eV = Ω_h - 2Ω_c = 1141.59\ng = 12.8417  ->  E_J = 8.6235\n4 jump operators (hot/cold emission and absorption)","title":"Building the model"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Two-currents,-one-factorization","page":"Circuit-QED heat engine","text":"This is the part of the API worth seeing. Both heat currents are built from the same four jump operators, weighted by the photon energy they carry:\n\nhot current: jumps [J₁, J₃] (emission into / absorption from the hot bath) with weights [-Ω_h, +Ω_h];\ncold current: jumps [J₂, J₄] with weights [-Ω_c, +Ω_c].\n\nThe sign convention makes langle J_alpharangle the heat flowing into bath alpha. Since the two currents differ only in mJ and nu, we prepare the Drazin solver once with prepare_fcs_context and call fcscumulants_recursive on that context for each current. Passing the TraceConstrainedSteadyState straight into prepare_fcs_context also hands over the preconditioner built during the steady-state solve, so the whole point costs a single factorization no matter how many currents we ask for.\n\nL  = liouvillian(model.H, model.J)\n\n# Steady state: returns rho_ss *and* the ILU factorization built to get it.\nss = trace_constrained_steadystate(L;\n    method       = :iterative,\n    τ            = 1e-3,      # ILU drop tolerance\n    shift_factor = 1e-6,      # diagonal shift keeping the factorization stable\n    rtol = 1e-10, atol = 1e-14, itmax = 200, memory = 60)\n\n# Prepare the Drazin solver once; `ss.Pl` is carried over, so no second ILU.\nctx = prepare_fcs_context(ss; method = :iterative,\n                          τ = 0.05, rtol = 1e-8, itmax = 300, memory = 60)\n\n# Same context, two different monitored currents.\nhot  = fcscumulants_recursive(ctx; mJ = [model.J[1], model.J[3]],\n                                   nu = [-Ωh, Ωh], nC = 2)\ncold = fcscumulants_recursive(ctx; mJ = [model.J[2], model.J[4]],\n                                   nu = [-params.Ωc, params.Ωc], nC = 2)\n\nJh, Dh = real(hot[1]),  real(hot[2])\nJc, Dc = real(cold[1]), real(cold[2])\n\n@printf(\"hot :  ⟨J_h⟩ = %12.6g   ⟨⟨J_h²⟩⟩ = %12.6g\\n\", Jh, Dh)\n@printf(\"cold:  ⟨J_c⟩ = %12.6g   ⟨⟨J_c²⟩⟩ = %12.6g\\n\", Jc, Dc)\n@printf(\"steady state converged in %d iterations (ILU %.2f s, GMRES %.2f s)\\n\",\n        ss.stats.iterations, ss.stats.ilu_seconds, ss.stats.gmres_seconds)\n\nhot :  ⟨J_h⟩ =      369.612   ⟨⟨J_h²⟩⟩ =       696376\ncold:  ⟨J_c⟩ =     -235.302   ⟨⟨J_c²⟩⟩ =       282231\nsteady state converged in 3 iterations (ILU 0.01 s, GMRES 1.04 s)\n\nHeat flows in from the hot bath and out into the cold one, as the signs show. Adding a third or fourth monitored current here would cost two more GMRES solves each and no additional factorization at all.","title":"Two currents, one factorization"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Consistency-checks","page":"Circuit-QED heat engine","text":"Two identities must hold, and both are checked at every point of the sweeps below.\n\nTight coupling. Because every tunnelling event moves exactly l_h hot and l_c cold photons, the two heat currents are rigidly locked:\n\nl_c Omega_c langle J_hrangle + l_h Omega_h langle J_crangle = 0 \n\nCurrent identity. The first FCS cumulant must equal the current computed directly from the steady state as a weighted sum of jump rates,\n\nc_1 = sum_k nu_k langle J_k^dagger J_krangle \n\nan independent route to the same number that exercises none of the Drazin machinery.\n\nρss = QuantumObject(ss.rho_ss; type = Operator, dims = model.dims)\n\n# The current straight from the steady state: Σ_k ν_k ⟨J_k† J_k⟩.\njump_current(ρ, jumps, weights) =\n    sum(real(weights[k] * expect(jumps[k]' * jumps[k], ρ)) for k in eachindex(jumps))\n\nhot_direct  = jump_current(ρss, [model.J[1], model.J[3]], [-Ωh, Ωh])\ncold_direct = jump_current(ρss, [model.J[2], model.J[4]], [-params.Ωc, params.Ωc])\n\ntight_coupling_error =\n    abs(params.lc * params.Ωc * Jh + params.lh * Ωh * Jc) /\n    max(abs(params.lc * params.Ωc * Jh), abs(params.lh * Ωh * Jc), eps())\n\n@printf(\"tight coupling  l_c Ω_c ⟨J_h⟩ + l_h Ω_h ⟨J_c⟩ = 0  ->  rel. error %.2e\\n\",\n        tight_coupling_error)\n@printf(\"hot  current identity  c₁/direct − 1 = %.2e\\n\", Jh / hot_direct - 1)\n@printf(\"cold current identity  c₁/direct − 1 = %.2e\\n\", Jc / cold_direct - 1)\n\ntight coupling  l_c Ω_c ⟨J_h⟩ + l_h Ω_h ⟨J_c⟩ = 0  ->  rel. error 6.67e-10\nhot  current identity  c₁/direct − 1 = 3.33e-15\ncold current identity  c₁/direct − 1 = -3.33e-16","title":"Consistency checks"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Thermodynamics:-affinity,-Fano-factors,-and-the-TUR","page":"Circuit-QED heat engine","text":"The thermodynamic force driving the engine is the affinity\n\nmathcalA = frac1l_hlnleft(frac1bar n_c+1right)\n            - frac1l_clnleft(frac1bar n_h+1right)\n\nwhich sets the entropy production associated with each current,\n\nsigma_h = l_c fraclangle J_hrangleOmega_hmathcalA\nqquad\nsigma_c = -l_h fraclangle J_crangleOmega_cmathcalA \n\nThe thermodynamic uncertainty relation states that the uncertainty product obeys\n\nQ_alpha\n= fraclanglelangle J_alpha^2rangleranglelangle J_alpharangle^2\n  sigma_alpha ge 2 \n\nRewriting it as a bound on the Fano factor mathcalF_alpha = langlelangle J_alpha^2ranglerangle  (Omega_alpha langle J_alpharangle) gives a threshold\n\nmathcalF_h^mathrmTUR = frac2l_c mathcalA\nqquad\nmathcalF_c^mathrmTUR = frac2l_h mathcalA \n\nHere is the point of the large-affinity regime: that threshold drops well below the Poisson value 1, so an engine can be strongly sub-Poissonian — antibunched — and still respect the TUR.\n\n𝒜  = log(1 / params.nc + 1) / params.lh - log(1 / params.nh + 1) / params.lc\nσh = params.lc * (Jh / Ωh) * 𝒜\nσc = -params.lh * (Jc / params.Ωc) * 𝒜\n\nFh = abs(Dh / (Ωh * Jh))\nFc = abs(Dc / (params.Ωc * Jc))\nQh = (Dh / Jh^2) * σh\nQc = (Dc / Jc^2) * σc\n\nFh_TUR = 2 / (params.lc * 𝒜)\nFc_TUR = 2 / (params.lh * 𝒜)\n\nP_drive = Jh + Jc\n\n@printf(\"affinity        𝒜 = %.4f\\n\", 𝒜)\n@printf(\"hot  Fano       ℱ_h = %.4f      TUR threshold ℱ_h^TUR = %.4f\\n\", Fh, Fh_TUR)\n@printf(\"cold Fano       ℱ_c = %.4f      TUR threshold ℱ_c^TUR = %.4f\\n\", Fc, Fc_TUR)\n@printf(\"uncertainty     Q_h = %.4f,  Q_c = %.4f      (TUR bound: 2)\\n\", Qh, Qc)\n@printf(\"output power    ⟨P⟩ = %.6g\\n\", P_drive)\n\nif Fh < 1 && Qh > 2\n    println(\"\\n→ sub-Poissonian (ℱ_h < 1) yet Q_h > 2: antibunching without a TUR violation.\")\nend\n\naffinity        𝒜 = 4.0658\nhot  Fano       ℱ_h = 0.5997      TUR threshold ℱ_h^TUR = 0.2460\ncold Fano       ℱ_c = 1.1994      TUR threshold ℱ_c^TUR = 0.4919\nuncertainty     Q_h = 4.8767,  Q_c = 4.8767      (TUR bound: 2)\noutput power    ⟨P⟩ = 134.31\n\n→ sub-Poissonian (ℱ_h < 1) yet Q_h > 2: antibunching without a TUR violation.\n\nThe hot current is sub-Poissonian at mathcalF_h approx 060 while its TUR threshold sits at 0246: there is a factor of sim24 of room. Note that Q_h = Q_c exactly — a consequence of tight coupling, which makes the two currents proportional and their uncertainty products identical.","title":"Thermodynamics: affinity, Fano factors, and the TUR"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Sweeping-parameters","page":"Circuit-QED heat engine","text":"Everything above, packaged for one parameter set, so a sweep is a map over it. Each point rebuilds the Liouvillian and hence needs its own factorization — but within a point, the steady state and both currents still share it.\n\nfunction qhe_point(p)\n    m  = qhe_model(p)\n    Ωh = p.Ωratio * p.Ωc\n\n    ss  = trace_constrained_steadystate(liouvillian(m.H, m.J);\n        method = :iterative, τ = 1e-3, shift_factor = 1e-6,\n        rtol = 1e-10, atol = 1e-14, itmax = 200, memory = 60)\n    ctx = prepare_fcs_context(ss; method = :iterative,\n                              τ = 0.05, rtol = 1e-8, itmax = 300, memory = 60)\n\n    hot  = fcscumulants_recursive(ctx; mJ = [m.J[1], m.J[3]], nu = [-Ωh, Ωh], nC = 2)\n    cold = fcscumulants_recursive(ctx; mJ = [m.J[2], m.J[4]], nu = [-p.Ωc, p.Ωc], nC = 2)\n    Jh, Dh = real(hot[1]),  real(hot[2])\n    Jc, Dc = real(cold[1]), real(cold[2])\n\n    𝒜  = log(1 / p.nc + 1) / p.lh - log(1 / p.nh + 1) / p.lc\n    σh = p.lc * (Jh / Ωh) * 𝒜\n    σc = -p.lh * (Jc / p.Ωc) * 𝒜\n\n    ρss = QuantumObject(ss.rho_ss; type = Operator, dims = m.dims)\n\n    return (; p.g, p.λc, Ej = m.Ej, Jh, Dh, Jc, Dc, 𝒜,\n              Fh = abs(Dh / (Ωh * Jh)), Fc = abs(Dc / (p.Ωc * Jc)),\n              Qh = (Dh / Jh^2) * σh,    Qc = (Dc / Jc^2) * σc,\n              Fh_TUR = 2 / (p.lc * 𝒜),  Fc_TUR = 2 / (p.lh * 𝒜),\n              P_drive = Jh + Jc,\n              diagnostics(ρss, m, p, Ωh, Jh, Jc)...)\nend\n\nThe diagnostics recorded alongside every point are what makes the sweep auditable — the truncation tails, the occupation fractions, the tight-coupling error, and varepsilon_textoff, the size of the off-resonant terms the RWA discards relative to their detuning delta = (Omega_hOmega_c - 3)Omega_c:\n\nfunction diagnostics(ρss, m, p, Ωh, Jh, Jc)\n    hot_pop  = max.(real.(diag(ptrace(ρss, 1).data)), 0.0);  hot_pop  ./= sum(hot_pop)\n    cold_pop = max.(real.(diag(ptrace(ρss, 2).data)), 0.0);  cold_pop ./= sum(cold_pop)\n\n    # Off-resonant terms the RWA discards, relative to their detuning.\n    δ  = abs((p.Ωratio - 3) * p.Ωc)\n    A0 = tensor(laguerre_operator(p.λh, 0, p.Nmax_h + 1),\n                laguerre_operator(p.λc, 1, p.Nmax_c + 1))\n    F  = -m.Ej / 2 * (im * m.ac * A0)\n    ε_off = sqrt(max(real(expect(F * F' + F' * F, ρss)), 0.0)) / δ\n\n    return (; hot_tail  = sum(hot_pop[end-1:end]),\n              cold_tail = sum(cold_pop[end-1:end]),\n              hot_occupation  = sum((0:p.Nmax_h) .* hot_pop) / p.Nmax_h,\n              cold_occupation = sum((0:p.Nmax_c) .* cold_pop) / p.Nmax_c,\n              ε_off,\n              tight_coupling_error =\n                  abs(p.lc * p.Ωc * Jh + p.lh * Ωh * Jc) /\n                  max(abs(p.lc * p.Ωc * Jh), abs(p.lh * Ωh * Jc), eps()))\nend\n\nTwo sweeps at fixed bath parameters: one over the coupling g at lambda_c = 089, one over lambda_c at the selected g. The lambda_c sweep needs a slightly larger cold cutoff. At 64 dimensions a point costs tens of milliseconds, so both sweeps finish in well under a minute.\n\n# Wrapped in a function: keeps the loop type-stable and lets a sweep be rerun.\nfunction qhe_sweep(base, values, setter; label = \"sweep\", progress_every = 50)\n    rows = NamedTuple[]\n    t0 = time_ns()\n    for (i, v) in enumerate(values)\n        pt = qhe_point(merge(base, setter(v)))\n        push!(rows, pt)\n        if i == 1 || i == length(values) || i % progress_every == 0\n            @printf(\"%-12s %3d/%3d  x=%7.4f | ℱ_h=%7.4f ℱ_c=%7.4f Q_h=%7.3f | tails %.1e/%.1e | %5.1fs\\n\",\n                    label, i, length(values), v, pt.Fh, pt.Fc, pt.Qh,\n                    pt.hot_tail, pt.cold_tail, (time_ns() - t0) / 1e9)\n        end\n    end\n    return rows\nend\n\nsweep_g = qhe_sweep(params, LinRange(1.0, 20.0, 200), v -> (; g = v);\n                    label = \"g sweep\", progress_every = 40)\n\nsweep_λc = qhe_sweep(merge(params, (; Nmax_c = 10)), LinRange(0.1, 2.0, 60),\n                     v -> (; λc = v); label = \"λc sweep\", progress_every = 12)\n\nbest = sweep_g[argmin(r.Fh for r in sweep_g)]\n@printf(\"\\nmin ℱ_h over the g sweep: %.4f at g = %.3f  (TUR threshold %.4f)\\n\",\n        best.Fh, best.g, best.Fh_TUR)\n\ng sweep        1/200  x= 1.0000 | ℱ_h= 0.9825 ℱ_c= 1.9651 Q_h=  7.990 | tails 1.2e-03/2.7e-10 |   0.7s\ng sweep       40/200  x= 4.7236 | ℱ_h= 0.7598 ℱ_c= 1.5196 Q_h=  6.178 | tails 1.2e-03/2.2e-07 |   3.9s\ng sweep       80/200  x= 8.5427 | ℱ_h= 0.6274 ℱ_c= 1.2549 Q_h=  5.102 | tails 1.1e-03/3.3e-06 |   6.6s\ng sweep      120/200  x=12.3618 | ℱ_h= 0.5999 ℱ_c= 1.1999 Q_h=  4.878 | tails 1.0e-03/1.6e-05 |   8.9s\ng sweep      160/200  x=16.1809 | ℱ_h= 0.6069 ℱ_c= 1.2139 Q_h=  4.935 | tails 1.0e-03/4.4e-05 |  11.4s\ng sweep      200/200  x=20.0000 | ℱ_h= 0.6234 ℱ_c= 1.2468 Q_h=  5.069 | tails 9.9e-04/9.4e-05 |  14.0s\nλc sweep       1/ 60  x= 0.1000 | ℱ_h= 0.8384 ℱ_c= 1.6769 Q_h=  6.818 | tails 7.8e-04/7.6e-04 |   0.4s\nλc sweep      12/ 60  x= 0.4542 | ℱ_h= 0.7993 ℱ_c= 1.5985 Q_h=  6.499 | tails 8.4e-04/9.9e-06 |   2.9s\nλc sweep      24/ 60  x= 0.8407 | ℱ_h= 0.5949 ℱ_c= 1.1898 Q_h=  4.838 | tails 1.0e-03/3.9e-08 |   4.5s\nλc sweep      36/ 60  x= 1.2271 | ℱ_h= 0.9638 ℱ_c= 1.9276 Q_h=  7.837 | tails 1.2e-03/2.9e-11 |   6.1s\nλc sweep      48/ 60  x= 1.6136 | ℱ_h= 1.0285 ℱ_c= 2.0570 Q_h=  8.363 | tails 1.2e-03/2.9e-14 |   8.1s\nλc sweep      60/ 60  x= 2.0000 | ℱ_h= 1.0027 ℱ_c= 2.0051 Q_h=  8.153 | tails 1.2e-03/1.3e-16 |   8.9s\n\nmin ℱ_h over the g sweep: 0.5997 at g = 12.839  (TUR threshold 0.2460)\n\nThe hot Fano factor dips to approx 060 — clearly sub-Poissonian — while its TUR threshold sits near 025. The engine is antibunched with room to spare above the bound, and the sweep locates the optimum at the g used for the manuscript's operating point.","title":"Sweeping parameters"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Validation","page":"Circuit-QED heat engine","text":"The RWA and the Fock truncation both have to be checked before the physics can be believed.\n\nfunction validation_summary(rows, xs, label; ε_tol = 0.05)\n    ok = [r.ε_off < ε_tol for r in rows]\n    @printf(\"%-9s | %3d/%3d points satisfy ε_off < %.2f | ε_off ∈ [%.1e, %.1e]\\n\",\n            label, count(ok), length(rows), ε_tol,\n            minimum(r.ε_off for r in rows), maximum(r.ε_off for r in rows))\n    @printf(\"          | tails: hot ≤ %.1e, cold ≤ %.1e | tight coupling ≤ %.1e\\n\",\n            maximum(r.hot_tail for r in rows), maximum(r.cold_tail for r in rows),\n            maximum(r.tight_coupling_error for r in rows))\n    all(ok) || @printf(\"          | RWA marginal for x ∈ [%.3f, %.3f]\\n\",\n                       minimum(xs[.!ok]), maximum(xs[.!ok]))\nend\n\nvalidation_summary(sweep_g,  [r.g  for r in sweep_g],  \"g sweep\")\nvalidation_summary(sweep_λc, [r.λc for r in sweep_λc], \"λc sweep\")\n\ng sweep   | 200/200 points satisfy ε_off < 0.05 | ε_off ∈ [2.7e-04, 6.6e-03]\n          | tails: hot ≤ 1.2e-03, cold ≤ 9.4e-05 | tight coupling ≤ 2.0e-08\nλc sweep  |  52/ 60 points satisfy ε_off < 0.05 | ε_off ∈ [3.5e-05, 4.2e-01]\n          | tails: hot ≤ 1.2e-03, cold ≤ 8.2e-04 | tight coupling ≤ 7.3e-06\n          | RWA marginal for x ∈ [0.100, 0.325]\n\nThe g sweep is clean throughout: varepsilon_textoff stays below 10^-2 at every point, truncation tails are at the 10^-3 level, and tight coupling holds to sim10^-8.\n\nThe lambda_c sweep is only trustworthy above lambda_c approx 033. This is not a numerical problem but a physical one: holding g fixed while shrinking lambda_c requires E_J = g(4lambda_hlambda_c^2) to grow without bound, so the off-resonant terms the RWA discards stop being small. The cutoffs remain perfectly adequate there — it is the model, not the solver, that degrades. The operating point used above, lambda_c = 089, sits comfortably inside the valid region.","title":"Validation"},{"category":"section","location":"examples/circuit_qed_heat_engine/#Cutoff-convergence","page":"Circuit-QED heat engine","text":"The Fock cutoffs here are fixed rather than scheduled (unlike the Jaynes–Cummings example), so they need justifying. Recompute one point on a ladder of cutoffs and watch the relative change from one rung to the next:\n\nfunction cutoff_convergence(base, cutoffs)\n    prev = nothing\n    println(\" Nmax_h  Nmax_c        J_h        ℱ_h        ℱ_c   ΔJ_h/J_h   Δℱ_h/ℱ_h   hot tail  cold tail\")\n    for (Nh, Nc) in cutoffs\n        r  = qhe_point(merge(base, (; Nmax_h = Nh, Nmax_c = Nc)))\n        dJ = prev === nothing ? NaN : abs(r.Jh / prev.Jh - 1)\n        dF = prev === nothing ? NaN : abs(r.Fh / prev.Fh - 1)\n        @printf(\"%7d %7d %10.6g %10.6f %10.6f %10.1e %10.1e %10.1e %10.1e\\n\",\n                Nh, Nc, r.Jh, r.Fh, r.Fc, dJ, dF, r.hot_tail, r.cold_tail)\n        prev = r\n    end\nend\n\ncutoff_convergence(params, [(5, 5), (7, 7), (9, 9), (11, 11)])\n\n Nmax_h  Nmax_c        J_h        ℱ_h        ℱ_c   ΔJ_h/J_h   Δℱ_h/ℱ_h   hot tail  cold tail\n      5       5    369.354   0.597924   1.195848        NaN        NaN    9.2e-03    6.2e-03\n      7       7    369.612   0.599720   1.199439    7.0e-04    3.0e-03    1.0e-03    1.8e-05\n      9       9    369.569   0.599676   1.199353    1.1e-04    7.2e-05    1.2e-04    2.5e-07\n     11      11    369.565   0.599677   1.199353    1.3e-05    4.2e-07    1.3e-05    1.2e-09\n\nThe changes fall by orders of magnitude as the cutoff grows, and the production cutoff N_max = 7 already sits in the converged region: going to 11 changes mathcalF_h in the fifth decimal, far below anything the figures claim.","title":"Cutoff convergence"},{"category":"section","location":"examples/circuit_qed_heat_engine/#The-manuscript-figure","page":"Circuit-QED heat engine","text":"(Image: Circuit-QED heat engine: antibunching at large affinity)\n\nThe published figure runs the two sweeps above at full resolution and shows the scaled heat currents and output power (a, e), the hot and cold Fano factors against their TUR thresholds and the Poisson line (b, c, f, g), and the RWA coherence (d, h).\n\nA second regime, not reproduced here, lowers the affinity by warming the cold bath (bar n_h = 25, bar n_c = 15). That raises the TUR thresholds far above 1 and lets the engine be pushed close to the bound Q approx 2 — near-reversible operation with small currents. It needs much larger cutoffs (N_maxh = 20, N_maxc = 25, a 546-dimensional Hilbert space), which is where the iterative backend starts to matter for this model too. Both regimes, the figure routines, and the checked-in sweep data live in the companion repository QuantumFCS-Notebooks.","title":"The manuscript figure"},{"category":"section","location":"examples/circuit_qed_heat_engine/#See-also","page":"Circuit-QED heat engine","text":"Driven-dissipative Jaynes–Cummings model — one current, but a Liouvillian four orders of magnitude larger, with preconditioner reuse carried across a parameter sweep.\nReusing a prepared solver across observables — the general form of the multi-current pattern used here.","title":"See also"}]
}
